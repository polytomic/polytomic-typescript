// This file was auto-generated by Fern from our API Definition.

import type { BaseClientOptions, BaseIdempotentRequestOptions, BaseRequestOptions } from "../../../../BaseClient";
import { type NormalizedClientOptionsWithAuth, normalizeClientOptionsWithAuth } from "../../../../BaseClient";
import * as core from "../../../../core";
import { mergeHeaders, mergeOnlyDefinedHeaders } from "../../../../core/headers";
import { toJson } from "../../../../core/json";
import * as environments from "../../../../environments";
import { handleNonStatusCodeError } from "../../../../errors/handleNonStatusCodeError";
import * as errors from "../../../../errors/index";
import * as Polytomic from "../../../index";

export declare namespace ModelsClient {
    export type Options = BaseClientOptions;

    export interface RequestOptions extends BaseRequestOptions {}

    export interface IdempotentRequestOptions extends RequestOptions, BaseIdempotentRequestOptions {}
}

export class ModelsClient {
    protected readonly _options: NormalizedClientOptionsWithAuth<ModelsClient.Options>;

    constructor(options: ModelsClient.Options = {}) {
        this._options = normalizeClientOptionsWithAuth(options);
    }

    /**
     * Describes the enrichment source configuration available on a connection.
     *
     * Not all connections support enrichment. Call this endpoint to determine
     * whether a connection can serve as an enrichment source in a model sync and,
     * if so, what configuration it accepts.
     *
     * > ⚠️ If the connection does not support enrichment, this endpoint returns
     * > `404`. Check for that status before attempting to configure an enrichment
     * > source on a sync.
     *
     * When a connection does support enrichment, the response describes the
     * configuration fields required to set it up. Pass those values in the
     * `enrichment` block when creating or updating a model sync.
     *
     * @param {string} id - Unique identifier of the connection.
     * @param {Polytomic.ModelsGetEnrichmentSourceRequest} request
     * @param {ModelsClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.BadRequestError}
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.InternalServerError}
     *
     * @example
     *     await client.models.getEnrichmentSource("248df4b7-aa70-47b8-a036-33ac447e668d")
     */
    public getEnrichmentSource(
        id: string,
        request: Polytomic.ModelsGetEnrichmentSourceRequest = {},
        requestOptions?: ModelsClient.RequestOptions,
    ): core.HttpResponsePromise<Polytomic.GetSyncSourceMetaEnvelope> {
        return core.HttpResponsePromise.fromPromise(this.__getEnrichmentSource(id, request, requestOptions));
    }

    private async __getEnrichmentSource(
        id: string,
        request: Polytomic.ModelsGetEnrichmentSourceRequest = {},
        requestOptions?: ModelsClient.RequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.GetSyncSourceMetaEnvelope>> {
        const { params } = request;
        const _queryParams: Record<string, unknown> = {
            params: params != null ? toJson(params) : undefined,
        };
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/connections/${core.url.encodePathParam(id)}/modelsync/enrichment-source`,
            ),
            method: "GET",
            headers: _headers,
            queryString: core.url
                .queryBuilder()
                .addMany(_queryParams)
                .mergeAdditional(requestOptions?.queryParams)
                .build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Polytomic.GetSyncSourceMetaEnvelope, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 400:
                    throw new Polytomic.BadRequestError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "GET",
            "/api/connections/{id}/modelsync/enrichment-source",
        );
    }

    /**
     * Returns the valid input field sets for an enrichment configuration on a connection.
     *
     * When configuring an enrichment source in a model sync, use this endpoint to
     * discover which input fields the enrichment connection requires. Pass the
     * proposed enrichment configuration in the request body; the response lists the
     * valid input field sets that map your model's fields to the enrichment service's
     * expected inputs.
     *
     * @param {string} connection_id
     * @param {Polytomic.EnrichmentInputFieldsRequest} request
     * @param {ModelsClient.IdempotentRequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.InternalServerError}
     *
     * @example
     *     await client.models.post("248df4b7-aa70-47b8-a036-33ac447e668d")
     */
    public post(
        connection_id: string,
        request: Polytomic.EnrichmentInputFieldsRequest = {},
        requestOptions?: ModelsClient.IdempotentRequestOptions,
    ): core.HttpResponsePromise<Polytomic.GetEnrichmentInputFieldsResponseEnvelope> {
        return core.HttpResponsePromise.fromPromise(this.__post(connection_id, request, requestOptions));
    }

    private async __post(
        connection_id: string,
        request: Polytomic.EnrichmentInputFieldsRequest = {},
        requestOptions?: ModelsClient.IdempotentRequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.GetEnrichmentInputFieldsResponseEnvelope>> {
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "Idempotency-Key": requestOptions?.idempotencyKey,
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/enrichment/${core.url.encodePathParam(connection_id)}/inputfields`,
            ),
            method: "POST",
            headers: _headers,
            contentType: "application/json",
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            requestType: "json",
            body: request,
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: _response.body as Polytomic.GetEnrichmentInputFieldsResponseEnvelope,
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "POST",
            "/api/enrichment/{connection_id}/inputfields",
        );
    }

    /**
     * Submits a job that previews the fields a model would expose without persisting it.
     *
     * The response contains a job ID that resolves to the list of fields the model
     * would expose. Poll the job until it completes to retrieve the field list. The
     * model is not persisted — this endpoint is useful for validating a query or
     * configuration before calling [`POST /api/models`](../../api-reference/models/create) to save it.
     *
     * @param {Polytomic.ModelsPreviewRequest} request
     * @param {ModelsClient.IdempotentRequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.BadRequestError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.InternalServerError}
     *
     * @example
     *     await client.models.preview({
     *         async: true,
     *         body: {
     *             configuration: {
     *                 "table": "public.users"
     *             },
     *             connection_id: "248df4b7-aa70-47b8-a036-33ac447e668d",
     *             name: "Users"
     *         }
     *     })
     */
    public preview(
        request: Polytomic.ModelsPreviewRequest,
        requestOptions?: ModelsClient.IdempotentRequestOptions,
    ): core.HttpResponsePromise<Polytomic.ModelResponseEnvelope> {
        return core.HttpResponsePromise.fromPromise(this.__preview(request, requestOptions));
    }

    private async __preview(
        request: Polytomic.ModelsPreviewRequest,
        requestOptions?: ModelsClient.IdempotentRequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.ModelResponseEnvelope>> {
        const { async, body: _body } = request;
        const _queryParams: Record<string, unknown> = {
            async,
        };
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "Idempotency-Key": requestOptions?.idempotencyKey,
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                "api/model-preview",
            ),
            method: "POST",
            headers: _headers,
            contentType: "application/json",
            queryString: core.url
                .queryBuilder()
                .addMany(_queryParams)
                .mergeAdditional(requestOptions?.queryParams)
                .build(),
            requestType: "json",
            body: _body,
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Polytomic.ModelResponseEnvelope, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 400:
                    throw new Polytomic.BadRequestError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "POST", "/api/model-preview");
    }

    /**
     * Lists all models in the caller's organization.
     *
     * Results are ordered by `updated_at` descending, with `id` used as a tiebreaker.
     * If more results are available, the response includes `pagination.next_page_token`.
     * Pass that token back unchanged to continue from the last item you received.
     *
     * The token is opaque. Do not construct or edit it yourself.
     *
     * The `limit` is capped at 50. Values above that cap are reduced to 50, and
     * non-positive values fall back to the same default.
     *
     * @param {ModelsClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.NotFoundError}
     *
     * @example
     *     await client.models.list()
     */
    public list(
        requestOptions?: ModelsClient.RequestOptions,
    ): core.HttpResponsePromise<Polytomic.ModelListResponseEnvelope> {
        return core.HttpResponsePromise.fromPromise(this.__list(requestOptions));
    }

    private async __list(
        requestOptions?: ModelsClient.RequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.ModelListResponseEnvelope>> {
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                "api/models",
            ),
            method: "GET",
            headers: _headers,
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Polytomic.ModelListResponseEnvelope, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "GET", "/api/models");
    }

    /**
     * Creates a new model.
     *
     * A model defines a query or view over a connection's data — for example, a SQL
     * query, a filtered object, or a joined dataset. Models are used as sources when
     * creating model syncs.
     *
     * The connection referenced by `connection_id` must have source capabilities. Use
     * [`GET /api/connection_types/{id}`](../../api-reference/connections/get-connection-type-schema) to check
     * whether a connection type supports use as a source.
     *
     * @param {Polytomic.ModelsCreateRequest} request
     * @param {ModelsClient.IdempotentRequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.BadRequestError}
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.InternalServerError}
     *
     * @example
     *     await client.models.create({
     *         async: true,
     *         body: {
     *             configuration: {
     *                 "table": "public.users"
     *             },
     *             connection_id: "248df4b7-aa70-47b8-a036-33ac447e668d",
     *             name: "Users"
     *         }
     *     })
     */
    public create(
        request: Polytomic.ModelsCreateRequest,
        requestOptions?: ModelsClient.IdempotentRequestOptions,
    ): core.HttpResponsePromise<Polytomic.ModelResponseEnvelope> {
        return core.HttpResponsePromise.fromPromise(this.__create(request, requestOptions));
    }

    private async __create(
        request: Polytomic.ModelsCreateRequest,
        requestOptions?: ModelsClient.IdempotentRequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.ModelResponseEnvelope>> {
        const { async, body: _body } = request;
        const _queryParams: Record<string, unknown> = {
            async,
        };
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "Idempotency-Key": requestOptions?.idempotencyKey,
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                "api/models",
            ),
            method: "POST",
            headers: _headers,
            contentType: "application/json",
            queryString: core.url
                .queryBuilder()
                .addMany(_queryParams)
                .mergeAdditional(requestOptions?.queryParams)
                .build(),
            requestType: "json",
            body: _body,
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Polytomic.ModelResponseEnvelope, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 400:
                    throw new Polytomic.BadRequestError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "POST", "/api/models");
    }

    /**
     * Returns a single model by ID, including its source fields, identity, and filters.
     *
     * The response includes the model's source fields, identity column, and any
     * configured filters. To preview the data a model would return without saving
     * changes, use [`GET /api/models/{id}/sample`](../../../api-reference/models/sample).
     *
     * @param {string} id
     * @param {Polytomic.ModelsGetRequest} request
     * @param {ModelsClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.InternalServerError}
     *
     * @example
     *     await client.models.get("248df4b7-aa70-47b8-a036-33ac447e668d", {
     *         async: true
     *     })
     */
    public get(
        id: string,
        request: Polytomic.ModelsGetRequest = {},
        requestOptions?: ModelsClient.RequestOptions,
    ): core.HttpResponsePromise<Polytomic.ModelResponseEnvelope> {
        return core.HttpResponsePromise.fromPromise(this.__get(id, request, requestOptions));
    }

    private async __get(
        id: string,
        request: Polytomic.ModelsGetRequest = {},
        requestOptions?: ModelsClient.RequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.ModelResponseEnvelope>> {
        const { async } = request;
        const _queryParams: Record<string, unknown> = {
            async,
        };
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/models/${core.url.encodePathParam(id)}`,
            ),
            method: "GET",
            headers: _headers,
            queryString: core.url
                .queryBuilder()
                .addMany(_queryParams)
                .mergeAdditional(requestOptions?.queryParams)
                .build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Polytomic.ModelResponseEnvelope, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "GET", "/api/models/{id}");
    }

    /**
     * Updates a model's configuration.
     *
     * Updating a model is a **full replacement** of its configuration. Every field in
     * the request body is written to the model; any field you omit is cleared or reset
     * to its default value.
     *
     * To make a partial change, fetch the current model with
     * [`GET /api/models/{id}`](../../../api-reference/models/get), modify the fields you want to change, and send
     * the complete object back in the update request.
     *
     * Changes to source fields, filters, or the identity column take effect on the
     * next sync execution that uses this model.
     *
     * @param {string} id
     * @param {Polytomic.UpdateModelRequest} request
     * @param {ModelsClient.IdempotentRequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.BadRequestError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.InternalServerError}
     *
     * @example
     *     await client.models.update("248df4b7-aa70-47b8-a036-33ac447e668d", {
     *         async: false,
     *         configuration: {
     *             "table": "public.users"
     *         },
     *         connection_id: "248df4b7-aa70-47b8-a036-33ac447e668d",
     *         name: "Users"
     *     })
     */
    public update(
        id: string,
        request: Polytomic.UpdateModelRequest,
        requestOptions?: ModelsClient.IdempotentRequestOptions,
    ): core.HttpResponsePromise<Polytomic.ModelResponseEnvelope> {
        return core.HttpResponsePromise.fromPromise(this.__update(id, request, requestOptions));
    }

    private async __update(
        id: string,
        request: Polytomic.UpdateModelRequest,
        requestOptions?: ModelsClient.IdempotentRequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.ModelResponseEnvelope>> {
        const { async, ..._body } = request;
        const _queryParams: Record<string, unknown> = {
            async,
        };
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "Idempotency-Key": requestOptions?.idempotencyKey,
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/models/${core.url.encodePathParam(id)}`,
            ),
            method: "PUT",
            headers: _headers,
            contentType: "application/json",
            queryString: core.url
                .queryBuilder()
                .addMany(_queryParams)
                .mergeAdditional(requestOptions?.queryParams)
                .build(),
            requestType: "json",
            body: _body,
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Polytomic.ModelResponseEnvelope, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 400:
                    throw new Polytomic.BadRequestError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "PUT", "/api/models/{id}");
    }

    /**
     * Deletes a model.
     *
     * > 🚧 Deleting a model used by one or more syncs will break those syncs. Remove
     * > or reconfigure any syncs that reference this model before deleting it.
     *
     * @param {string} id
     * @param {Polytomic.ModelsDeleteRequest} request
     * @param {ModelsClient.IdempotentRequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.InternalServerError}
     *
     * @example
     *     await client.models.delete("248df4b7-aa70-47b8-a036-33ac447e668d", {
     *         async: true
     *     })
     */
    public delete(
        id: string,
        request: Polytomic.ModelsDeleteRequest = {},
        requestOptions?: ModelsClient.IdempotentRequestOptions,
    ): core.HttpResponsePromise<void> {
        return core.HttpResponsePromise.fromPromise(this.__delete(id, request, requestOptions));
    }

    private async __delete(
        id: string,
        request: Polytomic.ModelsDeleteRequest = {},
        requestOptions?: ModelsClient.IdempotentRequestOptions,
    ): Promise<core.WithRawResponse<void>> {
        const { async } = request;
        const _queryParams: Record<string, unknown> = {
            async,
        };
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "Idempotency-Key": requestOptions?.idempotencyKey,
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/models/${core.url.encodePathParam(id)}`,
            ),
            method: "DELETE",
            headers: _headers,
            queryString: core.url
                .queryBuilder()
                .addMany(_queryParams)
                .mergeAdditional(requestOptions?.queryParams)
                .build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: undefined, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "DELETE", "/api/models/{id}");
    }

    /**
     * Returns a sample of records from a model.
     *
     * Synchronous requests must complete within 10 seconds. If the source query or
     * enrichment step can exceed that budget, use the asynchronous option so the
     * work runs as a background job.
     *
     * @param {string} id
     * @param {Polytomic.ModelsSampleRequest} request
     * @param {ModelsClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.BadRequestError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.InternalServerError}
     *
     * @example
     *     await client.models.sample("248df4b7-aa70-47b8-a036-33ac447e668d", {
     *         async: true
     *     })
     */
    public sample(
        id: string,
        request: Polytomic.ModelsSampleRequest = {},
        requestOptions?: ModelsClient.RequestOptions,
    ): core.HttpResponsePromise<Polytomic.ModelSampleResponseEnvelope> {
        return core.HttpResponsePromise.fromPromise(this.__sample(id, request, requestOptions));
    }

    private async __sample(
        id: string,
        request: Polytomic.ModelsSampleRequest = {},
        requestOptions?: ModelsClient.RequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.ModelSampleResponseEnvelope>> {
        const { async } = request;
        const _queryParams: Record<string, unknown> = {
            async,
        };
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/models/${core.url.encodePathParam(id)}/sample`,
            ),
            method: "GET",
            headers: _headers,
            queryString: core.url
                .queryBuilder()
                .addMany(_queryParams)
                .mergeAdditional(requestOptions?.queryParams)
                .build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: _response.body as Polytomic.ModelSampleResponseEnvelope,
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 400:
                    throw new Polytomic.BadRequestError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "GET", "/api/models/{id}/sample");
    }
}
