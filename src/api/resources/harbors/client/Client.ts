// This file was auto-generated by Fern from our API Definition.

import type { BaseClientOptions, BaseIdempotentRequestOptions, BaseRequestOptions } from "../../../../BaseClient";
import { type NormalizedClientOptionsWithAuth, normalizeClientOptionsWithAuth } from "../../../../BaseClient";
import * as core from "../../../../core";
import { mergeHeaders, mergeOnlyDefinedHeaders } from "../../../../core/headers";
import { mergeAdditionalBodyParameters } from "../../../../core/requestBody";
import * as environments from "../../../../environments";
import { handleNonStatusCodeError } from "../../../../errors/handleNonStatusCodeError";
import * as errors from "../../../../errors/index";
import * as Polytomic from "../../../index";

export declare namespace HarborsClient {
    export type Options = BaseClientOptions;

    export interface RequestOptions extends BaseRequestOptions {}

    export interface IdempotentRequestOptions extends RequestOptions, BaseIdempotentRequestOptions {}
}

export class HarborsClient {
    protected readonly _options: NormalizedClientOptionsWithAuth<HarborsClient.Options>;

    constructor(options: HarborsClient.Options = {}) {
        this._options = normalizeClientOptionsWithAuth(options);
    }

    /**
     * Lists profile-authorized connections and capabilities for the current Harbor credential.
     *
     * @param {Polytomic.HarborsListAuthorizedConnectionsRequest} request
     * @param {HarborsClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.listAuthorizedConnections({
     *         limit: 1,
     *         page_token: "page_token"
     *     })
     */
    public listAuthorizedConnections(
        request: Polytomic.HarborsListAuthorizedConnectionsRequest = {},
        requestOptions?: HarborsClient.RequestOptions,
    ): core.HttpResponsePromise<Polytomic.HarborConnectionListEnvelope> {
        return core.HttpResponsePromise.fromPromise(this.__listAuthorizedConnections(request, requestOptions));
    }

    private async __listAuthorizedConnections(
        request: Polytomic.HarborsListAuthorizedConnectionsRequest = {},
        requestOptions?: HarborsClient.RequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.HarborConnectionListEnvelope>> {
        const {
            limit,
            page_token: pageToken,
            "X-Polytomic-Harbor-Session": polytomicHarborSession,
            "X-Polytomic-Activity-Request-ID": polytomicActivityRequestId,
        } = request;
        const _queryParams: Record<string, unknown> = {
            limit,
            page_token: pageToken,
        };
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "X-Polytomic-Harbor-Session": polytomicHarborSession,
                "X-Polytomic-Activity-Request-ID": polytomicActivityRequestId,
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                "api/harbor/connections",
            ),
            method: "GET",
            headers: _headers,
            queryString: core.url
                .queryBuilder()
                .addMany(_queryParams)
                .mergeAdditional(requestOptions?.queryParams)
                .build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: _response.body as Polytomic.HarborConnectionListEnvelope,
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            throw new errors.PolytomicError({
                statusCode: _response.error.statusCode,
                body: _response.error.body,
                rawResponse: _response.rawResponse,
            });
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "GET", "/api/harbor/connections");
    }

    /**
     * Lists one bounded page of schema resources authorized by the current Harbor profile.
     *
     * @param {string} connection_id
     * @param {Polytomic.HarborsListAuthorizedSchemasRequest} request
     * @param {HarborsClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.listAuthorizedSchemas("248df4b7-aa70-47b8-a036-33ac447e668d", {
     *         limit: 1,
     *         page_token: "page_token"
     *     })
     */
    public listAuthorizedSchemas(
        connection_id: string,
        request: Polytomic.HarborsListAuthorizedSchemasRequest = {},
        requestOptions?: HarborsClient.RequestOptions,
    ): core.HttpResponsePromise<Polytomic.HarborSchemaListEnvelope> {
        return core.HttpResponsePromise.fromPromise(
            this.__listAuthorizedSchemas(connection_id, request, requestOptions),
        );
    }

    private async __listAuthorizedSchemas(
        connection_id: string,
        request: Polytomic.HarborsListAuthorizedSchemasRequest = {},
        requestOptions?: HarborsClient.RequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.HarborSchemaListEnvelope>> {
        const {
            limit,
            page_token: pageToken,
            "X-Polytomic-Harbor-Session": polytomicHarborSession,
            "X-Polytomic-Activity-Request-ID": polytomicActivityRequestId,
        } = request;
        const _queryParams: Record<string, unknown> = {
            limit,
            page_token: pageToken,
        };
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "X-Polytomic-Harbor-Session": polytomicHarborSession,
                "X-Polytomic-Activity-Request-ID": polytomicActivityRequestId,
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/harbor/connections/${core.url.encodePathParam(connection_id)}/schemas`,
            ),
            method: "GET",
            headers: _headers,
            queryString: core.url
                .queryBuilder()
                .addMany(_queryParams)
                .mergeAdditional(requestOptions?.queryParams)
                .build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Polytomic.HarborSchemaListEnvelope, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            throw new errors.PolytomicError({
                statusCode: _response.error.statusCode,
                body: _response.error.body,
                rawResponse: _response.rawResponse,
            });
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "GET",
            "/api/harbor/connections/{connection_id}/schemas",
        );
    }

    /**
     * Returns one schema resource authorized by the current Harbor profile.
     *
     * @param {string} connection_id
     * @param {string} schema_id
     * @param {Polytomic.HarborsGetAuthorizedSchemaRequest} request
     * @param {HarborsClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.getAuthorizedSchema("248df4b7-aa70-47b8-a036-33ac447e668d", "schema_id")
     */
    public getAuthorizedSchema(
        connection_id: string,
        schema_id: string,
        request: Polytomic.HarborsGetAuthorizedSchemaRequest = {},
        requestOptions?: HarborsClient.RequestOptions,
    ): core.HttpResponsePromise<Polytomic.HarborConnectionSchemaEnvelope> {
        return core.HttpResponsePromise.fromPromise(
            this.__getAuthorizedSchema(connection_id, schema_id, request, requestOptions),
        );
    }

    private async __getAuthorizedSchema(
        connection_id: string,
        schema_id: string,
        request: Polytomic.HarborsGetAuthorizedSchemaRequest = {},
        requestOptions?: HarborsClient.RequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.HarborConnectionSchemaEnvelope>> {
        const {
            "X-Polytomic-Harbor-Session": polytomicHarborSession,
            "X-Polytomic-Activity-Request-ID": polytomicActivityRequestId,
        } = request;
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "X-Polytomic-Harbor-Session": polytomicHarborSession,
                "X-Polytomic-Activity-Request-ID": polytomicActivityRequestId,
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/harbor/connections/${core.url.encodePathParam(connection_id)}/schemas/${core.url.encodePathParam(schema_id)}`,
            ),
            method: "GET",
            headers: _headers,
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: _response.body as Polytomic.HarborConnectionSchemaEnvelope,
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            throw new errors.PolytomicError({
                statusCode: _response.error.statusCode,
                body: _response.error.body,
                rawResponse: _response.rawResponse,
            });
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "GET",
            "/api/harbor/connections/{connection_id}/schemas/{schema_id}",
        );
    }

    /**
     * Registers a service-attested MCP transport session for a scoped Harbor credential.
     *
     * @param {Polytomic.RegisterHarborSessionRequest} request
     * @param {HarborsClient.IdempotentRequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.ServiceUnavailableError}
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.registerSession()
     */
    public registerSession(
        request: Polytomic.RegisterHarborSessionRequest = {},
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): core.HttpResponsePromise<Polytomic.RegisterHarborSessionEnvelope> {
        return core.HttpResponsePromise.fromPromise(this.__registerSession(request, requestOptions));
    }

    private async __registerSession(
        request: Polytomic.RegisterHarborSessionRequest = {},
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.RegisterHarborSessionEnvelope>> {
        const {
            "X-Polytomic-MCP-Key-ID": polytomicMcpKeyId,
            "X-Polytomic-MCP-Timestamp": polytomicMcpTimestamp,
            "X-Polytomic-MCP-Nonce": polytomicMcpNonce,
            "X-Polytomic-MCP-Signature": polytomicMcpSignature,
            "X-Polytomic-Activity-Request-ID": polytomicActivityRequestId,
            ..._body
        } = request;
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "X-Polytomic-MCP-Key-ID": polytomicMcpKeyId,
                "X-Polytomic-MCP-Timestamp": polytomicMcpTimestamp,
                "X-Polytomic-MCP-Nonce": polytomicMcpNonce,
                "X-Polytomic-MCP-Signature": polytomicMcpSignature,
                "X-Polytomic-Activity-Request-ID": polytomicActivityRequestId,
                "Idempotency-Key": requestOptions?.idempotencyKey,
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                "api/harbor/sessions",
            ),
            method: "POST",
            headers: _headers,
            contentType: "application/json",
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            requestType: "json",
            body: mergeAdditionalBodyParameters(_body, requestOptions?.additionalBodyParameters),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: _response.body as Polytomic.RegisterHarborSessionEnvelope,
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 503:
                    throw new Polytomic.ServiceUnavailableError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "POST", "/api/harbor/sessions");
    }

    /**
     * Closes a service-attested Harbor MCP transport session.
     *
     * @param {string} session_id
     * @param {Polytomic.HarborsCloseSessionRequest} request
     * @param {HarborsClient.IdempotentRequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.ServiceUnavailableError}
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.closeSession("248df4b7-aa70-47b8-a036-33ac447e668d")
     */
    public closeSession(
        session_id: string,
        request: Polytomic.HarborsCloseSessionRequest = {},
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): core.HttpResponsePromise<Polytomic.CloseHarborSessionEnvelope> {
        return core.HttpResponsePromise.fromPromise(this.__closeSession(session_id, request, requestOptions));
    }

    private async __closeSession(
        session_id: string,
        request: Polytomic.HarborsCloseSessionRequest = {},
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.CloseHarborSessionEnvelope>> {
        const {
            "X-Polytomic-MCP-Key-ID": polytomicMcpKeyId,
            "X-Polytomic-MCP-Timestamp": polytomicMcpTimestamp,
            "X-Polytomic-MCP-Nonce": polytomicMcpNonce,
            "X-Polytomic-MCP-Signature": polytomicMcpSignature,
            "X-Polytomic-Activity-Request-ID": polytomicActivityRequestId,
            "X-Polytomic-Harbor-Session": polytomicHarborSession,
        } = request;
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "X-Polytomic-MCP-Key-ID": polytomicMcpKeyId,
                "X-Polytomic-MCP-Timestamp": polytomicMcpTimestamp,
                "X-Polytomic-MCP-Nonce": polytomicMcpNonce,
                "X-Polytomic-MCP-Signature": polytomicMcpSignature,
                "X-Polytomic-Activity-Request-ID": polytomicActivityRequestId,
                "X-Polytomic-Harbor-Session": polytomicHarborSession,
                "Idempotency-Key": requestOptions?.idempotencyKey,
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/harbor/sessions/${core.url.encodePathParam(session_id)}`,
            ),
            method: "DELETE",
            headers: _headers,
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Polytomic.CloseHarborSessionEnvelope, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 503:
                    throw new Polytomic.ServiceUnavailableError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "DELETE",
            "/api/harbor/sessions/{session_id}",
        );
    }

    /**
     * Lists Harbors in the caller's current organization.
     *
     * Returns Harbors in creation order. Use `pagination.next_page_token` to continue when more results are available.
     *
     * @param {Polytomic.HarborsListRequest} request
     * @param {HarborsClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.UnprocessableEntityError}
     * @throws {@link Polytomic.InternalServerError}
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.list({
     *         limit: 50,
     *         page_token: "page_token"
     *     })
     */
    public list(
        request: Polytomic.HarborsListRequest = {},
        requestOptions?: HarborsClient.RequestOptions,
    ): core.HttpResponsePromise<Polytomic.HarborListEnvelope> {
        return core.HttpResponsePromise.fromPromise(this.__list(request, requestOptions));
    }

    private async __list(
        request: Polytomic.HarborsListRequest = {},
        requestOptions?: HarborsClient.RequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.HarborListEnvelope>> {
        const { limit, page_token: pageToken } = request;
        const _queryParams: Record<string, unknown> = {
            limit,
            page_token: pageToken,
        };
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                "api/harbors",
            ),
            method: "GET",
            headers: _headers,
            queryString: core.url
                .queryBuilder()
                .addMany(_queryParams)
                .mergeAdditional(requestOptions?.queryParams)
                .build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Polytomic.HarborListEnvelope, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 422:
                    throw new Polytomic.UnprocessableEntityError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "GET", "/api/harbors");
    }

    /**
     * Creates a managed or customer-managed Harbor in the caller's current organization.
     *
     * `generate_api_key` defaults to `true`. Polytomic returns a new plaintext credential only in this response. Set it to `false` to create the Harbor without a credential.
     *
     * For `customer_managed`, `backing_connection_id` must identify a queryable Connection that your credential can access.
     *
     * Managed Harbors return with `status: provisioning` while Polytomic sets up their
     * storage in the background. Poll `GET /api/harbors/{harbor_id}` until the status is
     * `ready` before adding sources or querying data. If provisioning fails, the status
     * is `provisioning_failed` and Polytomic retries automatically.
     *
     * @param {Polytomic.CreateHarborRequest} request
     * @param {HarborsClient.IdempotentRequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.ConflictError}
     * @throws {@link Polytomic.UnprocessableEntityError}
     * @throws {@link Polytomic.InternalServerError}
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.create({
     *         backing_mode: "managed",
     *         name: "Revenue Operations"
     *     })
     */
    public create(
        request: Polytomic.CreateHarborRequest,
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): core.HttpResponsePromise<Polytomic.CreateHarborEnvelope> {
        return core.HttpResponsePromise.fromPromise(this.__create(request, requestOptions));
    }

    private async __create(
        request: Polytomic.CreateHarborRequest,
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.CreateHarborEnvelope>> {
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "Idempotency-Key": requestOptions?.idempotencyKey,
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                "api/harbors",
            ),
            method: "POST",
            headers: _headers,
            contentType: "application/json",
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            requestType: "json",
            body: mergeAdditionalBodyParameters(request, requestOptions?.additionalBodyParameters),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Polytomic.CreateHarborEnvelope, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 409:
                    throw new Polytomic.ConflictError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 422:
                    throw new Polytomic.UnprocessableEntityError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "POST", "/api/harbors");
    }

    /**
     * Returns one Harbor by its first-class Harbor ID.
     *
     * The response exposes the backing Connection ID but not the internal profile used to authorize Harbor credentials.
     *
     * @param {string} harbor_id - Unique identifier of the Harbor.
     * @param {HarborsClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.InternalServerError}
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.get("248df4b7-aa70-47b8-a036-33ac447e668d")
     */
    public get(
        harbor_id: string,
        requestOptions?: HarborsClient.RequestOptions,
    ): core.HttpResponsePromise<Polytomic.HarborEnvelope> {
        return core.HttpResponsePromise.fromPromise(this.__get(harbor_id, requestOptions));
    }

    private async __get(
        harbor_id: string,
        requestOptions?: HarborsClient.RequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.HarborEnvelope>> {
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/harbors/${core.url.encodePathParam(harbor_id)}`,
            ),
            method: "GET",
            headers: _headers,
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Polytomic.HarborEnvelope, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "GET", "/api/harbors/{harbor_id}");
    }

    /**
     * Updates a Harbor's name and description.
     *
     * Renaming a managed Harbor also renames its backing Connection. Both names
     * change together; a name conflict returns `409 Conflict` and leaves both
     * unchanged. Renaming a customer-managed Harbor preserves its Connection's name.
     *
     * This operation does not change `backing_mode` or `backing_connection_id`.
     *
     * @param {string} harbor_id - Unique identifier of the Harbor.
     * @param {Polytomic.UpdateHarborRequest} request
     * @param {HarborsClient.IdempotentRequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.ConflictError}
     * @throws {@link Polytomic.UnprocessableEntityError}
     * @throws {@link Polytomic.InternalServerError}
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.update("248df4b7-aa70-47b8-a036-33ac447e668d", {
     *         name: "Revenue Operations"
     *     })
     */
    public update(
        harbor_id: string,
        request: Polytomic.UpdateHarborRequest,
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): core.HttpResponsePromise<Polytomic.HarborEnvelope> {
        return core.HttpResponsePromise.fromPromise(this.__update(harbor_id, request, requestOptions));
    }

    private async __update(
        harbor_id: string,
        request: Polytomic.UpdateHarborRequest,
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.HarborEnvelope>> {
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "Idempotency-Key": requestOptions?.idempotencyKey,
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/harbors/${core.url.encodePathParam(harbor_id)}`,
            ),
            method: "PUT",
            headers: _headers,
            contentType: "application/json",
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            requestType: "json",
            body: mergeAdditionalBodyParameters(request, requestOptions?.additionalBodyParameters),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Polytomic.HarborEnvelope, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 409:
                    throw new Polytomic.ConflictError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 422:
                    throw new Polytomic.UnprocessableEntityError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "PUT", "/api/harbors/{harbor_id}");
    }

    /**
     * Deletes a Harbor and revokes its credentials.
     *
     * > 🚧 Harbor deletion
     * >
     * > Deleting a Harbor revokes its credentials, context documents, and user assignments. A customer-managed backing Connection remains available. Polytomic deletes a managed backing Connection only when no other resource uses it.
     *
     * The response confirms that access has been revoked. Polytomic removes managed
     * storage in the background and retries failed cleanup automatically. You can also
     * delete a Harbor while it is provisioning.
     *
     * @param {string} harbor_id - Unique identifier of the Harbor.
     * @param {HarborsClient.IdempotentRequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.ConflictError}
     * @throws {@link Polytomic.InternalServerError}
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.delete("248df4b7-aa70-47b8-a036-33ac447e668d")
     */
    public delete(
        harbor_id: string,
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): core.HttpResponsePromise<Polytomic.DeletedHarborEnvelope> {
        return core.HttpResponsePromise.fromPromise(this.__delete(harbor_id, requestOptions));
    }

    private async __delete(
        harbor_id: string,
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.DeletedHarborEnvelope>> {
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "Idempotency-Key": requestOptions?.idempotencyKey,
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/harbors/${core.url.encodePathParam(harbor_id)}`,
            ),
            method: "DELETE",
            headers: _headers,
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Polytomic.DeletedHarborEnvelope, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 409:
                    throw new Polytomic.ConflictError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "DELETE", "/api/harbors/{harbor_id}");
    }

    /**
     * Lists context document metadata for a Harbor without returning document content.
     *
     * Collection items include the current published `version` number and omit
     * `content`. Use the context item endpoint to retrieve a complete document.
     *
     * A Harbor profile credential can read context only when `harbor_id` identifies
     * its own Harbor.
     *
     * @param {string} harbor_id - Unique identifier of the Harbor.
     * @param {Polytomic.HarborsListContextsRequest} request
     * @param {HarborsClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.UnprocessableEntityError}
     * @throws {@link Polytomic.InternalServerError}
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.listContexts("248df4b7-aa70-47b8-a036-33ac447e668d", {
     *         limit: 50,
     *         page_token: "page_token"
     *     })
     */
    public listContexts(
        harbor_id: string,
        request: Polytomic.HarborsListContextsRequest = {},
        requestOptions?: HarborsClient.RequestOptions,
    ): core.HttpResponsePromise<Polytomic.HarborContextListEnvelope> {
        return core.HttpResponsePromise.fromPromise(this.__listContexts(harbor_id, request, requestOptions));
    }

    private async __listContexts(
        harbor_id: string,
        request: Polytomic.HarborsListContextsRequest = {},
        requestOptions?: HarborsClient.RequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.HarborContextListEnvelope>> {
        const {
            limit,
            page_token: pageToken,
            "X-Polytomic-Harbor-Session": polytomicHarborSession,
            "X-Polytomic-Activity-Request-ID": polytomicActivityRequestId,
        } = request;
        const _queryParams: Record<string, unknown> = {
            limit,
            page_token: pageToken,
        };
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "X-Polytomic-Harbor-Session": polytomicHarborSession,
                "X-Polytomic-Activity-Request-ID": polytomicActivityRequestId,
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/harbors/${core.url.encodePathParam(harbor_id)}/contexts`,
            ),
            method: "GET",
            headers: _headers,
            queryString: core.url
                .queryBuilder()
                .addMany(_queryParams)
                .mergeAdditional(requestOptions?.queryParams)
                .build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Polytomic.HarborContextListEnvelope, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 422:
                    throw new Polytomic.UnprocessableEntityError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "GET",
            "/api/harbors/{harbor_id}/contexts",
        );
    }

    /**
     * Creates and attaches a context document to a Harbor.
     *
     * The new document belongs only to this Harbor. Context documents cannot be attached to multiple Harbors.
     *
     * @param {string} harbor_id - Unique identifier of the Harbor.
     * @param {Polytomic.CreateHarborContextRequest} request
     * @param {HarborsClient.IdempotentRequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.UnprocessableEntityError}
     * @throws {@link Polytomic.InternalServerError}
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.createContext("248df4b7-aa70-47b8-a036-33ac447e668d", {
     *         content: "Bookings use the contract signed date...",
     *         title: "Revenue definitions"
     *     })
     */
    public createContext(
        harbor_id: string,
        request: Polytomic.CreateHarborContextRequest,
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): core.HttpResponsePromise<Polytomic.HarborContextEnvelope> {
        return core.HttpResponsePromise.fromPromise(this.__createContext(harbor_id, request, requestOptions));
    }

    private async __createContext(
        harbor_id: string,
        request: Polytomic.CreateHarborContextRequest,
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.HarborContextEnvelope>> {
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "Idempotency-Key": requestOptions?.idempotencyKey,
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/harbors/${core.url.encodePathParam(harbor_id)}/contexts`,
            ),
            method: "POST",
            headers: _headers,
            contentType: "application/json",
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            requestType: "json",
            body: mergeAdditionalBodyParameters(request, requestOptions?.additionalBodyParameters),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Polytomic.HarborContextEnvelope, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 422:
                    throw new Polytomic.UnprocessableEntityError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "POST",
            "/api/harbors/{harbor_id}/contexts",
        );
    }

    /**
     * Lists mutable Harbor context drafts without returning document content.
     *
     * The collection includes drafts for published context documents and initial drafts
     * that have not yet been published. Use `context_id` with the draft detail,
     * replacement, promotion, and discard endpoints.
     *
     * Draft metadata does not include `content`. Fetch a selected draft through its
     * detail endpoint to read the complete candidate. Normal context list and detail
     * operations continue to return published content only.
     *
     * @param {string} harbor_id - Unique identifier of the Harbor.
     * @param {Polytomic.HarborsListContextDraftsRequest} request
     * @param {HarborsClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.UnprocessableEntityError}
     * @throws {@link Polytomic.InternalServerError}
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.listContextDrafts("248df4b7-aa70-47b8-a036-33ac447e668d", {
     *         limit: 50,
     *         page_token: "page_token"
     *     })
     */
    public listContextDrafts(
        harbor_id: string,
        request: Polytomic.HarborsListContextDraftsRequest = {},
        requestOptions?: HarborsClient.RequestOptions,
    ): core.HttpResponsePromise<Polytomic.HarborContextDraftListEnvelope> {
        return core.HttpResponsePromise.fromPromise(this.__listContextDrafts(harbor_id, request, requestOptions));
    }

    private async __listContextDrafts(
        harbor_id: string,
        request: Polytomic.HarborsListContextDraftsRequest = {},
        requestOptions?: HarborsClient.RequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.HarborContextDraftListEnvelope>> {
        const { limit, page_token: pageToken } = request;
        const _queryParams: Record<string, unknown> = {
            limit,
            page_token: pageToken,
        };
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/harbors/${core.url.encodePathParam(harbor_id)}/contexts/drafts`,
            ),
            method: "GET",
            headers: _headers,
            queryString: core.url
                .queryBuilder()
                .addMany(_queryParams)
                .mergeAdditional(requestOptions?.queryParams)
                .build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: _response.body as Polytomic.HarborContextDraftListEnvelope,
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 422:
                    throw new Polytomic.UnprocessableEntityError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "GET",
            "/api/harbors/{harbor_id}/contexts/drafts",
        );
    }

    /**
     * Creates an unpublished context document with its initial mutable draft.
     *
     * Creates a stable context identity and its initial mutable draft without publishing
     * content to the Harbor. The response includes `context_id`, which identifies the
     * draft replacement, promotion, and discard routes.
     *
     * The initial draft has a null `base_revision_id`. It remains absent from normal
     * context list/detail, GraphQL, entity lookup, and Harbor MCP reads until promoted.
     * Use the regular context creation endpoint instead when the initial payload should
     * be published immediately.
     *
     * @param {string} harbor_id - Unique identifier of the Harbor.
     * @param {Polytomic.CreateHarborContextDraftRequest} request
     * @param {HarborsClient.IdempotentRequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.UnprocessableEntityError}
     * @throws {@link Polytomic.InternalServerError}
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.createContextDraft("248df4b7-aa70-47b8-a036-33ac447e668d", {
     *         content: "Bookings use the contract signed date...",
     *         title: "Revenue definitions"
     *     })
     */
    public createContextDraft(
        harbor_id: string,
        request: Polytomic.CreateHarborContextDraftRequest,
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): core.HttpResponsePromise<Polytomic.HarborContextDraftEnvelope> {
        return core.HttpResponsePromise.fromPromise(this.__createContextDraft(harbor_id, request, requestOptions));
    }

    private async __createContextDraft(
        harbor_id: string,
        request: Polytomic.CreateHarborContextDraftRequest,
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.HarborContextDraftEnvelope>> {
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "Idempotency-Key": requestOptions?.idempotencyKey,
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/harbors/${core.url.encodePathParam(harbor_id)}/contexts/drafts`,
            ),
            method: "POST",
            headers: _headers,
            contentType: "application/json",
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            requestType: "json",
            body: mergeAdditionalBodyParameters(request, requestOptions?.additionalBodyParameters),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Polytomic.HarborContextDraftEnvelope, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 422:
                    throw new Polytomic.UnprocessableEntityError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "POST",
            "/api/harbors/{harbor_id}/contexts/drafts",
        );
    }

    /**
     * Returns one complete Harbor context document.
     *
     * The response includes the current published `version` number and complete
     * plain-text `content`.
     *
     * A Harbor profile credential can read context only when `harbor_id` identifies
     * its own Harbor.
     *
     * @param {string} harbor_id - Unique identifier of the Harbor.
     * @param {string} context_id - Unique identifier of the context document.
     * @param {Polytomic.HarborsGetContextRequest} request
     * @param {HarborsClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.InternalServerError}
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.getContext("248df4b7-aa70-47b8-a036-33ac447e668d", "248df4b7-aa70-47b8-a036-33ac447e668d")
     */
    public getContext(
        harbor_id: string,
        context_id: string,
        request: Polytomic.HarborsGetContextRequest = {},
        requestOptions?: HarborsClient.RequestOptions,
    ): core.HttpResponsePromise<Polytomic.HarborContextEnvelope> {
        return core.HttpResponsePromise.fromPromise(this.__getContext(harbor_id, context_id, request, requestOptions));
    }

    private async __getContext(
        harbor_id: string,
        context_id: string,
        request: Polytomic.HarborsGetContextRequest = {},
        requestOptions?: HarborsClient.RequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.HarborContextEnvelope>> {
        const {
            "X-Polytomic-Harbor-Session": polytomicHarborSession,
            "X-Polytomic-Activity-Request-ID": polytomicActivityRequestId,
        } = request;
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "X-Polytomic-Harbor-Session": polytomicHarborSession,
                "X-Polytomic-Activity-Request-ID": polytomicActivityRequestId,
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/harbors/${core.url.encodePathParam(harbor_id)}/contexts/${core.url.encodePathParam(context_id)}`,
            ),
            method: "GET",
            headers: _headers,
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Polytomic.HarborContextEnvelope, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "GET",
            "/api/harbors/{harbor_id}/contexts/{context_id}",
        );
    }

    /**
     * Replaces one Harbor context document.
     *
     * Each successful request publishes the next immutable version. You can omit
     * `change_note`.
     *
     * A direct publication leaves an existing draft unchanged.
     *
     * @param {string} harbor_id - Unique identifier of the Harbor.
     * @param {string} context_id - Unique identifier of the context document.
     * @param {Polytomic.SaveHarborContextRequest} request
     * @param {HarborsClient.IdempotentRequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.UnprocessableEntityError}
     * @throws {@link Polytomic.InternalServerError}
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.updateContext("248df4b7-aa70-47b8-a036-33ac447e668d", "248df4b7-aa70-47b8-a036-33ac447e668d", {
     *         content: "Bookings use the contract signed date...",
     *         title: "Revenue definitions"
     *     })
     */
    public updateContext(
        harbor_id: string,
        context_id: string,
        request: Polytomic.SaveHarborContextRequest,
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): core.HttpResponsePromise<Polytomic.HarborContextEnvelope> {
        return core.HttpResponsePromise.fromPromise(
            this.__updateContext(harbor_id, context_id, request, requestOptions),
        );
    }

    private async __updateContext(
        harbor_id: string,
        context_id: string,
        request: Polytomic.SaveHarborContextRequest,
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.HarborContextEnvelope>> {
        const {
            "X-Polytomic-Harbor-Session": polytomicHarborSession,
            "X-Polytomic-Activity-Request-ID": polytomicActivityRequestId,
            ..._body
        } = request;
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "X-Polytomic-Harbor-Session": polytomicHarborSession,
                "X-Polytomic-Activity-Request-ID": polytomicActivityRequestId,
                "Idempotency-Key": requestOptions?.idempotencyKey,
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/harbors/${core.url.encodePathParam(harbor_id)}/contexts/${core.url.encodePathParam(context_id)}`,
            ),
            method: "PUT",
            headers: _headers,
            contentType: "application/json",
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            requestType: "json",
            body: mergeAdditionalBodyParameters(_body, requestOptions?.additionalBodyParameters),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Polytomic.HarborContextEnvelope, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 422:
                    throw new Polytomic.UnprocessableEntityError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "PUT",
            "/api/harbors/{harbor_id}/contexts/{context_id}",
        );
    }

    /**
     * Deletes one context document from a Harbor.
     *
     * Deleting a context document does not affect the Harbor or its other context documents.
     *
     * @param {string} harbor_id - Unique identifier of the Harbor.
     * @param {string} context_id - Unique identifier of the context document.
     * @param {Polytomic.HarborsDeleteContextRequest} request
     * @param {HarborsClient.IdempotentRequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.InternalServerError}
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.deleteContext("248df4b7-aa70-47b8-a036-33ac447e668d", "248df4b7-aa70-47b8-a036-33ac447e668d")
     */
    public deleteContext(
        harbor_id: string,
        context_id: string,
        request: Polytomic.HarborsDeleteContextRequest = {},
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): core.HttpResponsePromise<Polytomic.DeletedHarborContextEnvelope> {
        return core.HttpResponsePromise.fromPromise(
            this.__deleteContext(harbor_id, context_id, request, requestOptions),
        );
    }

    private async __deleteContext(
        harbor_id: string,
        context_id: string,
        request: Polytomic.HarborsDeleteContextRequest = {},
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.DeletedHarborContextEnvelope>> {
        const {
            "X-Polytomic-Harbor-Session": polytomicHarborSession,
            "X-Polytomic-Activity-Request-ID": polytomicActivityRequestId,
        } = request;
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "X-Polytomic-Harbor-Session": polytomicHarborSession,
                "X-Polytomic-Activity-Request-ID": polytomicActivityRequestId,
                "Idempotency-Key": requestOptions?.idempotencyKey,
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/harbors/${core.url.encodePathParam(harbor_id)}/contexts/${core.url.encodePathParam(context_id)}`,
            ),
            method: "DELETE",
            headers: _headers,
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: _response.body as Polytomic.DeletedHarborContextEnvelope,
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "DELETE",
            "/api/harbors/{harbor_id}/contexts/{context_id}",
        );
    }

    /**
     * Returns the mutable draft for a Harbor context document.
     *
     * Drafts are available only through the draft endpoints. `base_revision_id`
     * identifies the published revision from which the candidate was created. Normal
     * context reads and Harbor MCP tools continue to return the current published
     * version. Creator and updater IDs are null when their actor type is `system`.
     *
     * @param {string} harbor_id - Unique identifier of the Harbor.
     * @param {string} context_id - Unique identifier of the context document.
     * @param {Polytomic.HarborsGetContextDraftRequest} request
     * @param {HarborsClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.InternalServerError}
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.getContextDraft("248df4b7-aa70-47b8-a036-33ac447e668d", "248df4b7-aa70-47b8-a036-33ac447e668d")
     */
    public getContextDraft(
        harbor_id: string,
        context_id: string,
        request: Polytomic.HarborsGetContextDraftRequest = {},
        requestOptions?: HarborsClient.RequestOptions,
    ): core.HttpResponsePromise<Polytomic.HarborContextDraftEnvelope> {
        return core.HttpResponsePromise.fromPromise(
            this.__getContextDraft(harbor_id, context_id, request, requestOptions),
        );
    }

    private async __getContextDraft(
        harbor_id: string,
        context_id: string,
        request: Polytomic.HarborsGetContextDraftRequest = {},
        requestOptions?: HarborsClient.RequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.HarborContextDraftEnvelope>> {
        const {
            "X-Polytomic-Harbor-Session": polytomicHarborSession,
            "X-Polytomic-Activity-Request-ID": polytomicActivityRequestId,
        } = request;
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "X-Polytomic-Harbor-Session": polytomicHarborSession,
                "X-Polytomic-Activity-Request-ID": polytomicActivityRequestId,
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/harbors/${core.url.encodePathParam(harbor_id)}/contexts/${core.url.encodePathParam(context_id)}/draft`,
            ),
            method: "GET",
            headers: _headers,
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Polytomic.HarborContextDraftEnvelope, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "GET",
            "/api/harbors/{harbor_id}/contexts/{context_id}/draft",
        );
    }

    /**
     * Creates or completely replaces the mutable draft for a Harbor context document.
     *
     * The request supplies the complete draft payload. If a draft already exists,
     * this request replaces it while preserving the draft ID, creation metadata, and
     * `base_revision_id`.
     *
     * A new draft for a published document records the current revision as its base.
     * An unpublished document's initial draft keeps a null base when replaced. Saving
     * a draft does not change published content or its `updated_at`. Creator and
     * updater provenance comes from the request actor; system actors have a null actor
     * ID.
     *
     * @param {string} harbor_id - Unique identifier of the Harbor.
     * @param {string} context_id - Unique identifier of the context document.
     * @param {Polytomic.SaveHarborContextDraftRequest} request
     * @param {HarborsClient.IdempotentRequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.UnprocessableEntityError}
     * @throws {@link Polytomic.InternalServerError}
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.saveContextDraft("248df4b7-aa70-47b8-a036-33ac447e668d", "248df4b7-aa70-47b8-a036-33ac447e668d", {
     *         content: "Bookings use the contract signed date...",
     *         title: "Revenue definitions"
     *     })
     */
    public saveContextDraft(
        harbor_id: string,
        context_id: string,
        request: Polytomic.SaveHarborContextDraftRequest,
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): core.HttpResponsePromise<Polytomic.HarborContextDraftEnvelope> {
        return core.HttpResponsePromise.fromPromise(
            this.__saveContextDraft(harbor_id, context_id, request, requestOptions),
        );
    }

    private async __saveContextDraft(
        harbor_id: string,
        context_id: string,
        request: Polytomic.SaveHarborContextDraftRequest,
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.HarborContextDraftEnvelope>> {
        const {
            "X-Polytomic-Harbor-Session": polytomicHarborSession,
            "X-Polytomic-Activity-Request-ID": polytomicActivityRequestId,
            ..._body
        } = request;
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "X-Polytomic-Harbor-Session": polytomicHarborSession,
                "X-Polytomic-Activity-Request-ID": polytomicActivityRequestId,
                "Idempotency-Key": requestOptions?.idempotencyKey,
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/harbors/${core.url.encodePathParam(harbor_id)}/contexts/${core.url.encodePathParam(context_id)}/draft`,
            ),
            method: "PUT",
            headers: _headers,
            contentType: "application/json",
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            requestType: "json",
            body: mergeAdditionalBodyParameters(_body, requestOptions?.additionalBodyParameters),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Polytomic.HarborContextDraftEnvelope, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 422:
                    throw new Polytomic.UnprocessableEntityError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "PUT",
            "/api/harbors/{harbor_id}/contexts/{context_id}/draft",
        );
    }

    /**
     * Discards the mutable draft for a Harbor context document.
     *
     * Discarding a draft does not change the current published version or its
     * history. If the draft belongs to a context that has never been published,
     * discarding it also removes the otherwise empty context identity.
     *
     * @param {string} harbor_id - Unique identifier of the Harbor.
     * @param {string} context_id - Unique identifier of the context document.
     * @param {Polytomic.HarborsDeleteContextDraftRequest} request
     * @param {HarborsClient.IdempotentRequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.InternalServerError}
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.deleteContextDraft("248df4b7-aa70-47b8-a036-33ac447e668d", "248df4b7-aa70-47b8-a036-33ac447e668d")
     */
    public deleteContextDraft(
        harbor_id: string,
        context_id: string,
        request: Polytomic.HarborsDeleteContextDraftRequest = {},
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): core.HttpResponsePromise<Polytomic.DeletedHarborContextDraftEnvelope> {
        return core.HttpResponsePromise.fromPromise(
            this.__deleteContextDraft(harbor_id, context_id, request, requestOptions),
        );
    }

    private async __deleteContextDraft(
        harbor_id: string,
        context_id: string,
        request: Polytomic.HarborsDeleteContextDraftRequest = {},
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.DeletedHarborContextDraftEnvelope>> {
        const {
            "X-Polytomic-Harbor-Session": polytomicHarborSession,
            "X-Polytomic-Activity-Request-ID": polytomicActivityRequestId,
        } = request;
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "X-Polytomic-Harbor-Session": polytomicHarborSession,
                "X-Polytomic-Activity-Request-ID": polytomicActivityRequestId,
                "Idempotency-Key": requestOptions?.idempotencyKey,
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/harbors/${core.url.encodePathParam(harbor_id)}/contexts/${core.url.encodePathParam(context_id)}/draft`,
            ),
            method: "DELETE",
            headers: _headers,
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: _response.body as Polytomic.DeletedHarborContextDraftEnvelope,
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "DELETE",
            "/api/harbors/{harbor_id}/contexts/{context_id}/draft",
        );
    }

    /**
     * Promotes the draft to the next immutable published context version.
     *
     * Promotion publishes the draft's exact title, description, content, and optional
     * change note as the next version. An unpublished context's initial draft becomes
     * version 1. The draft is removed after publication.
     *
     * Promotion returns a conflict when the current published revision differs from
     * the draft's `base_revision_id`. The stale draft remains available so an author
     * can compare it with the current version before discarding and recreating it.
     *
     * @param {string} harbor_id - Unique identifier of the Harbor.
     * @param {string} context_id - Unique identifier of the context document.
     * @param {Polytomic.HarborsPromoteContextDraftRequest} request
     * @param {HarborsClient.IdempotentRequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.ConflictError}
     * @throws {@link Polytomic.InternalServerError}
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.promoteContextDraft("248df4b7-aa70-47b8-a036-33ac447e668d", "248df4b7-aa70-47b8-a036-33ac447e668d")
     */
    public promoteContextDraft(
        harbor_id: string,
        context_id: string,
        request: Polytomic.HarborsPromoteContextDraftRequest = {},
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): core.HttpResponsePromise<Polytomic.HarborContextVersionEnvelope> {
        return core.HttpResponsePromise.fromPromise(
            this.__promoteContextDraft(harbor_id, context_id, request, requestOptions),
        );
    }

    private async __promoteContextDraft(
        harbor_id: string,
        context_id: string,
        request: Polytomic.HarborsPromoteContextDraftRequest = {},
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.HarborContextVersionEnvelope>> {
        const {
            "X-Polytomic-Harbor-Session": polytomicHarborSession,
            "X-Polytomic-Activity-Request-ID": polytomicActivityRequestId,
        } = request;
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "X-Polytomic-Harbor-Session": polytomicHarborSession,
                "X-Polytomic-Activity-Request-ID": polytomicActivityRequestId,
                "Idempotency-Key": requestOptions?.idempotencyKey,
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/harbors/${core.url.encodePathParam(harbor_id)}/contexts/${core.url.encodePathParam(context_id)}/draft/promote`,
            ),
            method: "POST",
            headers: _headers,
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: _response.body as Polytomic.HarborContextVersionEnvelope,
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 409:
                    throw new Polytomic.ConflictError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "POST",
            "/api/harbors/{harbor_id}/contexts/{context_id}/draft/promote",
        );
    }

    /**
     * Lists immutable published versions of a Harbor context document without returning content.
     *
     * Versions are ordered from newest to oldest. Collection items omit `content`,
     * and the draft is never included. Pagination tokens continue from the last
     * returned version, so publishing a newer version between requests does not shift
     * or duplicate older results.
     *
     * Published revision IDs are durable artifact identities intended for future
     * Harbor activity and audit records. Publisher IDs are paired with actor types.
     * System publications have `published_by_type: "system"` and a null
     * `published_by` because the system actor has no UUID.
     *
     * A Harbor profile credential can read versions only when `harbor_id` identifies
     * its own Harbor.
     *
     * @param {string} harbor_id - Unique identifier of the Harbor.
     * @param {string} context_id - Unique identifier of the context document.
     * @param {Polytomic.HarborsListContextVersionsRequest} request
     * @param {HarborsClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.UnprocessableEntityError}
     * @throws {@link Polytomic.InternalServerError}
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.listContextVersions("248df4b7-aa70-47b8-a036-33ac447e668d", "248df4b7-aa70-47b8-a036-33ac447e668d", {
     *         limit: 50,
     *         page_token: "page_token"
     *     })
     */
    public listContextVersions(
        harbor_id: string,
        context_id: string,
        request: Polytomic.HarborsListContextVersionsRequest = {},
        requestOptions?: HarborsClient.RequestOptions,
    ): core.HttpResponsePromise<Polytomic.HarborContextVersionListEnvelope> {
        return core.HttpResponsePromise.fromPromise(
            this.__listContextVersions(harbor_id, context_id, request, requestOptions),
        );
    }

    private async __listContextVersions(
        harbor_id: string,
        context_id: string,
        request: Polytomic.HarborsListContextVersionsRequest = {},
        requestOptions?: HarborsClient.RequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.HarborContextVersionListEnvelope>> {
        const {
            limit,
            page_token: pageToken,
            "X-Polytomic-Harbor-Session": polytomicHarborSession,
            "X-Polytomic-Activity-Request-ID": polytomicActivityRequestId,
        } = request;
        const _queryParams: Record<string, unknown> = {
            limit,
            page_token: pageToken,
        };
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "X-Polytomic-Harbor-Session": polytomicHarborSession,
                "X-Polytomic-Activity-Request-ID": polytomicActivityRequestId,
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/harbors/${core.url.encodePathParam(harbor_id)}/contexts/${core.url.encodePathParam(context_id)}/versions`,
            ),
            method: "GET",
            headers: _headers,
            queryString: core.url
                .queryBuilder()
                .addMany(_queryParams)
                .mergeAdditional(requestOptions?.queryParams)
                .build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: _response.body as Polytomic.HarborContextVersionListEnvelope,
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 422:
                    throw new Polytomic.UnprocessableEntityError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "GET",
            "/api/harbors/{harbor_id}/contexts/{context_id}/versions",
        );
    }

    /**
     * Returns one immutable published version of a Harbor context document.
     *
     * The response includes the complete title, description, and plain-text
     * `content` captured when the version was published.
     *
     * Published versions cannot be changed or deleted. System publications have
     * `published_by_type: "system"` and a null `published_by`.
     *
     * @param {string} harbor_id - Unique identifier of the Harbor.
     * @param {string} context_id - Unique identifier of the context document.
     * @param {string} version_id - Unique identifier of the published context version.
     * @param {Polytomic.HarborsGetContextVersionRequest} request
     * @param {HarborsClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.InternalServerError}
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.getContextVersion("248df4b7-aa70-47b8-a036-33ac447e668d", "248df4b7-aa70-47b8-a036-33ac447e668d", "248df4b7-aa70-47b8-a036-33ac447e668d")
     */
    public getContextVersion(
        harbor_id: string,
        context_id: string,
        version_id: string,
        request: Polytomic.HarborsGetContextVersionRequest = {},
        requestOptions?: HarborsClient.RequestOptions,
    ): core.HttpResponsePromise<Polytomic.HarborContextVersionEnvelope> {
        return core.HttpResponsePromise.fromPromise(
            this.__getContextVersion(harbor_id, context_id, version_id, request, requestOptions),
        );
    }

    private async __getContextVersion(
        harbor_id: string,
        context_id: string,
        version_id: string,
        request: Polytomic.HarborsGetContextVersionRequest = {},
        requestOptions?: HarborsClient.RequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.HarborContextVersionEnvelope>> {
        const {
            "X-Polytomic-Harbor-Session": polytomicHarborSession,
            "X-Polytomic-Activity-Request-ID": polytomicActivityRequestId,
        } = request;
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "X-Polytomic-Harbor-Session": polytomicHarborSession,
                "X-Polytomic-Activity-Request-ID": polytomicActivityRequestId,
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/harbors/${core.url.encodePathParam(harbor_id)}/contexts/${core.url.encodePathParam(context_id)}/versions/${core.url.encodePathParam(version_id)}`,
            ),
            method: "GET",
            headers: _headers,
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: _response.body as Polytomic.HarborContextVersionEnvelope,
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "GET",
            "/api/harbors/{harbor_id}/contexts/{context_id}/versions/{version_id}",
        );
    }

    /**
     * Lists active masked credentials for a Harbor.
     *
     * Each item contains a masked `key_hint`. Polytomic never returns a credential plaintext after creation.
     *
     * @param {string} harbor_id - Unique identifier of the Harbor.
     * @param {Polytomic.HarborsListKeysRequest} request
     * @param {HarborsClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.UnprocessableEntityError}
     * @throws {@link Polytomic.InternalServerError}
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.listKeys("248df4b7-aa70-47b8-a036-33ac447e668d", {
     *         limit: 50,
     *         page_token: "page_token"
     *     })
     */
    public listKeys(
        harbor_id: string,
        request: Polytomic.HarborsListKeysRequest = {},
        requestOptions?: HarborsClient.RequestOptions,
    ): core.HttpResponsePromise<Polytomic.HarborKeyListEnvelope> {
        return core.HttpResponsePromise.fromPromise(this.__listKeys(harbor_id, request, requestOptions));
    }

    private async __listKeys(
        harbor_id: string,
        request: Polytomic.HarborsListKeysRequest = {},
        requestOptions?: HarborsClient.RequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.HarborKeyListEnvelope>> {
        const { limit, page_token: pageToken } = request;
        const _queryParams: Record<string, unknown> = {
            limit,
            page_token: pageToken,
        };
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/harbors/${core.url.encodePathParam(harbor_id)}/keys`,
            ),
            method: "GET",
            headers: _headers,
            queryString: core.url
                .queryBuilder()
                .addMany(_queryParams)
                .mergeAdditional(requestOptions?.queryParams)
                .build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Polytomic.HarborKeyListEnvelope, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 422:
                    throw new Polytomic.UnprocessableEntityError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(_response.error, _response.rawResponse, "GET", "/api/harbors/{harbor_id}/keys");
    }

    /**
     * Generates a new Harbor credential and returns its plaintext value once.
     *
     * Store the returned `value` securely. Polytomic returns it only in this response.
     *
     * @param {string} harbor_id - Unique identifier of the Harbor.
     * @param {HarborsClient.IdempotentRequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.InternalServerError}
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.createKey("248df4b7-aa70-47b8-a036-33ac447e668d")
     */
    public createKey(
        harbor_id: string,
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): core.HttpResponsePromise<Polytomic.HarborKeyEnvelope> {
        return core.HttpResponsePromise.fromPromise(this.__createKey(harbor_id, requestOptions));
    }

    private async __createKey(
        harbor_id: string,
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.HarborKeyEnvelope>> {
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "Idempotency-Key": requestOptions?.idempotencyKey,
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/harbors/${core.url.encodePathParam(harbor_id)}/keys`,
            ),
            method: "POST",
            headers: _headers,
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Polytomic.HarborKeyEnvelope, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "POST",
            "/api/harbors/{harbor_id}/keys",
        );
    }

    /**
     * Revokes one Harbor credential by its credential ID.
     *
     * Revocation affects only the selected credential. Other active Harbor credentials remain valid.
     *
     * @param {string} harbor_id - Unique identifier of the Harbor.
     * @param {string} key_id - Unique identifier of the Harbor credential.
     * @param {HarborsClient.IdempotentRequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.InternalServerError}
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.deleteKey("248df4b7-aa70-47b8-a036-33ac447e668d", "248df4b7-aa70-47b8-a036-33ac447e668d")
     */
    public deleteKey(
        harbor_id: string,
        key_id: string,
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): core.HttpResponsePromise<Polytomic.RevokedHarborKeyEnvelope> {
        return core.HttpResponsePromise.fromPromise(this.__deleteKey(harbor_id, key_id, requestOptions));
    }

    private async __deleteKey(
        harbor_id: string,
        key_id: string,
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.RevokedHarborKeyEnvelope>> {
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "Idempotency-Key": requestOptions?.idempotencyKey,
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/harbors/${core.url.encodePathParam(harbor_id)}/keys/${core.url.encodePathParam(key_id)}`,
            ),
            method: "DELETE",
            headers: _headers,
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Polytomic.RevokedHarborKeyEnvelope, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "DELETE",
            "/api/harbors/{harbor_id}/keys/{key_id}",
        );
    }

    /**
     * Lists current published saved queries for a Harbor.
     *
     * Saved queries are supported only for Polytomic-managed Harbors whose backing
     * Connection type is `polytomic_harbor`.
     *
     * Collection items contain current published metadata and omit SQL, parameter
     * values, and unpublished drafts. A Harbor profile credential can list saved
     * queries only when `harbor_id` identifies its own Harbor.
     *
     * ## Harbor Activity
     *
     * When you use a Harbor profile credential, send a new nonzero UUID in
     * `X-Polytomic-Activity-Request-ID` for each request, including each page. A
     * missing or invalid ID returns `400 Bad Request`. Reusing a consumed ID returns
     * `409 Conflict`.
     *
     * If you supply `X-Polytomic-Harbor-Session`, the session must be active and bound
     * to your credential and Harbor. An invalid session returns `403 Forbidden`.
     * Direct scoped REST requests may omit the session header.
     *
     * Polytomic records `saved_query.listed` before returning data. The event describes
     * only the returned page, including saved-query IDs, immutable revision IDs,
     * version numbers, and bounded name snapshots. Activity excludes SQL, parameter
     * values, and expected column names. If Polytomic cannot record the event, the
     * request returns `503 Service Unavailable` without saved-query data.
     *
     * @param {string} harbor_id - Unique identifier of the Harbor.
     * @param {Polytomic.HarborsListSavedQueriesRequest} request
     * @param {HarborsClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.BadRequestError}
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.ConflictError}
     * @throws {@link Polytomic.UnprocessableEntityError}
     * @throws {@link Polytomic.InternalServerError}
     * @throws {@link Polytomic.ServiceUnavailableError}
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.listSavedQueries("248df4b7-aa70-47b8-a036-33ac447e668d", {
     *         limit: 1,
     *         page_token: "page_token"
     *     })
     */
    public listSavedQueries(
        harbor_id: string,
        request: Polytomic.HarborsListSavedQueriesRequest = {},
        requestOptions?: HarborsClient.RequestOptions,
    ): core.HttpResponsePromise<Polytomic.HarborSavedQueryListEnvelope> {
        return core.HttpResponsePromise.fromPromise(this.__listSavedQueries(harbor_id, request, requestOptions));
    }

    private async __listSavedQueries(
        harbor_id: string,
        request: Polytomic.HarborsListSavedQueriesRequest = {},
        requestOptions?: HarborsClient.RequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.HarborSavedQueryListEnvelope>> {
        const {
            limit,
            page_token: pageToken,
            "X-Polytomic-Harbor-Session": polytomicHarborSession,
            "X-Polytomic-Activity-Request-ID": polytomicActivityRequestId,
        } = request;
        const _queryParams: Record<string, unknown> = {
            limit,
            page_token: pageToken,
        };
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "X-Polytomic-Harbor-Session": polytomicHarborSession,
                "X-Polytomic-Activity-Request-ID": polytomicActivityRequestId,
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/harbors/${core.url.encodePathParam(harbor_id)}/saved-queries`,
            ),
            method: "GET",
            headers: _headers,
            queryString: core.url
                .queryBuilder()
                .addMany(_queryParams)
                .mergeAdditional(requestOptions?.queryParams)
                .build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: _response.body as Polytomic.HarborSavedQueryListEnvelope,
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 400:
                    throw new Polytomic.BadRequestError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 409:
                    throw new Polytomic.ConflictError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 422:
                    throw new Polytomic.UnprocessableEntityError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 503:
                    throw new Polytomic.ServiceUnavailableError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "GET",
            "/api/harbors/{harbor_id}/saved-queries",
        );
    }

    /**
     * Creates a stable Harbor saved query with its initial mutable draft.
     *
     * Saved queries are supported only for Polytomic-managed Harbors whose backing
     * Connection type is `polytomic_harbor`. Customer-managed Harbor backings return
     * an unsupported-backing error.
     *
     * The saved query receives a stable ID, but it remains absent from published
     * saved-query reads until an administrator publishes its initial draft.
     *
     * Use `{{parameter_name}}` references in `sql_template`. Each reference must have
     * one scalar declaration. Validation values and defaults must match the declared
     * `string`, `number`, `boolean`, `date`, `timestamp`, or `uuid` type.
     *
     * Unknown JSON fields, including fields inside parameter declarations, return
     * `400 Bad Request` before the draft is saved. Use `default_value`, not `default`,
     * for parameter defaults.
     *
     * When you omit a parameter during execution, Polytomic uses its published
     * `default_value`, never its draft validation value. An omitted optional parameter
     * without a default binds SQL `NULL`. A required parameter allows omission when a
     * default exists, but rejects explicit `null` even with a default.
     *
     * For a report window, use a required parameter with a default so omission selects
     * a useful window and explicit `null` is rejected:
     *
     * ```json
     * {"name": "days", "type": "number", "required": true, "default_value": 7}
     * ```
     *
     * @param {string} harbor_id - Unique identifier of the Harbor.
     * @param {Polytomic.CreateHarborSavedQueryDraftRequest} request
     * @param {HarborsClient.IdempotentRequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.BadRequestError}
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.UnprocessableEntityError}
     * @throws {@link Polytomic.InternalServerError}
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.createSavedQueryDraft("248df4b7-aa70-47b8-a036-33ac447e668d", {
     *         name: "Monthly revenue",
     *         sql_template: "SELECT account_id, sum(amount) AS revenue FROM orders WHERE created_at >= {{start_date}} GROUP BY account_id"
     *     })
     */
    public createSavedQueryDraft(
        harbor_id: string,
        request: Polytomic.CreateHarborSavedQueryDraftRequest,
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): core.HttpResponsePromise<Polytomic.HarborSavedQueryDraftEnvelope> {
        return core.HttpResponsePromise.fromPromise(this.__createSavedQueryDraft(harbor_id, request, requestOptions));
    }

    private async __createSavedQueryDraft(
        harbor_id: string,
        request: Polytomic.CreateHarborSavedQueryDraftRequest,
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.HarborSavedQueryDraftEnvelope>> {
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "Idempotency-Key": requestOptions?.idempotencyKey,
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/harbors/${core.url.encodePathParam(harbor_id)}/saved-queries`,
            ),
            method: "POST",
            headers: _headers,
            contentType: "application/json",
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            requestType: "json",
            body: mergeAdditionalBodyParameters(request, requestOptions?.additionalBodyParameters),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: _response.body as Polytomic.HarborSavedQueryDraftEnvelope,
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 400:
                    throw new Polytomic.BadRequestError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 422:
                    throw new Polytomic.UnprocessableEntityError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "POST",
            "/api/harbors/{harbor_id}/saved-queries",
        );
    }

    /**
     * Lists mutable Harbor saved-query drafts.
     *
     * Drafts are ordered from newest to oldest and include complete SQL, typed
     * parameter declarations, and author-supplied validation values. Harbor profile
     * credentials cannot access this endpoint.
     *
     * @param {string} harbor_id - Unique identifier of the Harbor.
     * @param {Polytomic.HarborsListSavedQueryDraftsRequest} request
     * @param {HarborsClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.UnprocessableEntityError}
     * @throws {@link Polytomic.InternalServerError}
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.listSavedQueryDrafts("248df4b7-aa70-47b8-a036-33ac447e668d", {
     *         limit: 1,
     *         page_token: "page_token"
     *     })
     */
    public listSavedQueryDrafts(
        harbor_id: string,
        request: Polytomic.HarborsListSavedQueryDraftsRequest = {},
        requestOptions?: HarborsClient.RequestOptions,
    ): core.HttpResponsePromise<Polytomic.HarborSavedQueryDraftListEnvelope> {
        return core.HttpResponsePromise.fromPromise(this.__listSavedQueryDrafts(harbor_id, request, requestOptions));
    }

    private async __listSavedQueryDrafts(
        harbor_id: string,
        request: Polytomic.HarborsListSavedQueryDraftsRequest = {},
        requestOptions?: HarborsClient.RequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.HarborSavedQueryDraftListEnvelope>> {
        const { limit, page_token: pageToken } = request;
        const _queryParams: Record<string, unknown> = {
            limit,
            page_token: pageToken,
        };
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/harbors/${core.url.encodePathParam(harbor_id)}/saved-queries/drafts`,
            ),
            method: "GET",
            headers: _headers,
            queryString: core.url
                .queryBuilder()
                .addMany(_queryParams)
                .mergeAdditional(requestOptions?.queryParams)
                .build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: _response.body as Polytomic.HarborSavedQueryDraftListEnvelope,
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 422:
                    throw new Polytomic.UnprocessableEntityError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "GET",
            "/api/harbors/{harbor_id}/saved-queries/drafts",
        );
    }

    /**
     * Returns the current published version of one Harbor saved query.
     *
     * The response contains the current immutable `revision_id`, SQL template,
     * parameter contract, expected columns, and publication provenance. Validation
     * inputs are draft-only and are never part of a published response.
     *
     * A Harbor profile credential can retrieve a saved query only from its own Harbor.
     *
     * ## Harbor Activity
     *
     * When you use a Harbor profile credential, send a new nonzero UUID in
     * `X-Polytomic-Activity-Request-ID` for each request. A missing or invalid ID
     * returns `400 Bad Request`. Reusing a consumed ID returns `409 Conflict`.
     *
     * If you supply `X-Polytomic-Harbor-Session`, the session must be active and bound
     * to your credential and Harbor. An invalid session returns `403 Forbidden`.
     * Direct scoped REST requests may omit the session header.
     *
     * Polytomic records `saved_query.fetched` before returning the definition. The
     * event identifies the saved query, exact immutable revision, published version,
     * and bounded name snapshot. It does not indicate query execution. Activity
     * excludes SQL, parameter defaults and validation values, expected column names,
     * and result rows. If Polytomic cannot record the event, the request returns
     * `503 Service Unavailable` without saved-query data.
     *
     * @param {string} harbor_id - Unique identifier of the Harbor.
     * @param {string} saved_query_id - Unique stable identifier of the saved query.
     * @param {Polytomic.HarborsGetSavedQueryRequest} request
     * @param {HarborsClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.BadRequestError}
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.ConflictError}
     * @throws {@link Polytomic.UnprocessableEntityError}
     * @throws {@link Polytomic.InternalServerError}
     * @throws {@link Polytomic.ServiceUnavailableError}
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.getSavedQuery("248df4b7-aa70-47b8-a036-33ac447e668d", "248df4b7-aa70-47b8-a036-33ac447e668d")
     */
    public getSavedQuery(
        harbor_id: string,
        saved_query_id: string,
        request: Polytomic.HarborsGetSavedQueryRequest = {},
        requestOptions?: HarborsClient.RequestOptions,
    ): core.HttpResponsePromise<Polytomic.HarborSavedQueryEnvelope> {
        return core.HttpResponsePromise.fromPromise(
            this.__getSavedQuery(harbor_id, saved_query_id, request, requestOptions),
        );
    }

    private async __getSavedQuery(
        harbor_id: string,
        saved_query_id: string,
        request: Polytomic.HarborsGetSavedQueryRequest = {},
        requestOptions?: HarborsClient.RequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.HarborSavedQueryEnvelope>> {
        const {
            "X-Polytomic-Harbor-Session": polytomicHarborSession,
            "X-Polytomic-Activity-Request-ID": polytomicActivityRequestId,
        } = request;
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "X-Polytomic-Harbor-Session": polytomicHarborSession,
                "X-Polytomic-Activity-Request-ID": polytomicActivityRequestId,
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/harbors/${core.url.encodePathParam(harbor_id)}/saved-queries/${core.url.encodePathParam(saved_query_id)}`,
            ),
            method: "GET",
            headers: _headers,
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Polytomic.HarborSavedQueryEnvelope, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 400:
                    throw new Polytomic.BadRequestError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 409:
                    throw new Polytomic.ConflictError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 422:
                    throw new Polytomic.UnprocessableEntityError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 503:
                    throw new Polytomic.ServiceUnavailableError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "GET",
            "/api/harbors/{harbor_id}/saved-queries/{saved_query_id}",
        );
    }

    /**
     * Archives one Harbor saved query and removes it from active reads.
     *
     * Archiving removes the saved query and any draft from active reads while
     * retaining its immutable revisions for provenance. The operation does not run
     * the saved SQL or modify the Harbor backing Connection. Archiving does not cancel
     * in-flight execution requests or queued executions.
     *
     * This REST operation archives the saved query; it does not merely unpublish it.
     * Unpublish is available only through the GraphQL `unpublishHarborSavedQuery`
     * mutation. There is no REST unpublish endpoint.
     *
     * @param {string} harbor_id - Unique identifier of the Harbor.
     * @param {string} saved_query_id - Unique stable identifier of the saved query.
     * @param {HarborsClient.IdempotentRequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.UnprocessableEntityError}
     * @throws {@link Polytomic.InternalServerError}
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.deleteSavedQuery("248df4b7-aa70-47b8-a036-33ac447e668d", "248df4b7-aa70-47b8-a036-33ac447e668d")
     */
    public deleteSavedQuery(
        harbor_id: string,
        saved_query_id: string,
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): core.HttpResponsePromise<Polytomic.DeletedHarborSavedQueryEnvelope> {
        return core.HttpResponsePromise.fromPromise(this.__deleteSavedQuery(harbor_id, saved_query_id, requestOptions));
    }

    private async __deleteSavedQuery(
        harbor_id: string,
        saved_query_id: string,
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.DeletedHarborSavedQueryEnvelope>> {
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "Idempotency-Key": requestOptions?.idempotencyKey,
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/harbors/${core.url.encodePathParam(harbor_id)}/saved-queries/${core.url.encodePathParam(saved_query_id)}`,
            ),
            method: "DELETE",
            headers: _headers,
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: _response.body as Polytomic.DeletedHarborSavedQueryEnvelope,
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 422:
                    throw new Polytomic.UnprocessableEntityError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "DELETE",
            "/api/harbors/{harbor_id}/saved-queries/{saved_query_id}",
        );
    }

    /**
     * Creates or completely replaces the mutable draft for a Harbor saved query.
     *
     * Replacement is complete rather than partial. The next validation or publication
     * uses the replacement SQL, parameter declarations, and validation values.
     *
     * Unknown JSON fields, including fields inside parameter declarations, return
     * `400 Bad Request` before the draft is saved. Use `default_value`, not `default`,
     * for parameter defaults.
     *
     * When you omit a parameter during execution, Polytomic uses its published
     * `default_value`, never its draft validation value. An omitted optional parameter
     * without a default binds SQL `NULL`. A required parameter allows omission when a
     * default exists, but rejects explicit `null` even with a default.
     *
     * For a report window, use a required parameter with a default so omission selects
     * a useful window and explicit `null` is rejected:
     *
     * ```json
     * {"name": "days", "type": "number", "required": true, "default_value": 7}
     * ```
     *
     * @param {string} harbor_id - Unique identifier of the Harbor.
     * @param {string} saved_query_id - Unique stable identifier of the saved query.
     * @param {Polytomic.SaveHarborSavedQueryDraftRequest} request
     * @param {HarborsClient.IdempotentRequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.BadRequestError}
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.UnprocessableEntityError}
     * @throws {@link Polytomic.InternalServerError}
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.saveSavedQueryDraft("248df4b7-aa70-47b8-a036-33ac447e668d", "248df4b7-aa70-47b8-a036-33ac447e668d", {
     *         name: "Monthly revenue",
     *         sql_template: "SELECT account_id, sum(amount) AS revenue FROM orders WHERE created_at >= {{start_date}} GROUP BY account_id"
     *     })
     */
    public saveSavedQueryDraft(
        harbor_id: string,
        saved_query_id: string,
        request: Polytomic.SaveHarborSavedQueryDraftRequest,
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): core.HttpResponsePromise<Polytomic.HarborSavedQueryDraftEnvelope> {
        return core.HttpResponsePromise.fromPromise(
            this.__saveSavedQueryDraft(harbor_id, saved_query_id, request, requestOptions),
        );
    }

    private async __saveSavedQueryDraft(
        harbor_id: string,
        saved_query_id: string,
        request: Polytomic.SaveHarborSavedQueryDraftRequest,
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.HarborSavedQueryDraftEnvelope>> {
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "Idempotency-Key": requestOptions?.idempotencyKey,
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/harbors/${core.url.encodePathParam(harbor_id)}/saved-queries/${core.url.encodePathParam(saved_query_id)}/draft`,
            ),
            method: "PUT",
            headers: _headers,
            contentType: "application/json",
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            requestType: "json",
            body: mergeAdditionalBodyParameters(request, requestOptions?.additionalBodyParameters),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: _response.body as Polytomic.HarborSavedQueryDraftEnvelope,
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 400:
                    throw new Polytomic.BadRequestError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 422:
                    throw new Polytomic.UnprocessableEntityError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "PUT",
            "/api/harbors/{harbor_id}/saved-queries/{saved_query_id}/draft",
        );
    }

    /**
     * Discards the mutable draft for one Harbor saved query.
     *
     * Discarding an initial unpublished draft also removes its otherwise empty stable
     * saved-query identity. Discarding a later draft preserves every immutable
     * published version.
     *
     * @param {string} harbor_id - Unique identifier of the Harbor.
     * @param {string} saved_query_id - Unique stable identifier of the saved query.
     * @param {HarborsClient.IdempotentRequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.ConflictError}
     * @throws {@link Polytomic.UnprocessableEntityError}
     * @throws {@link Polytomic.InternalServerError}
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.deleteSavedQueryDraft("248df4b7-aa70-47b8-a036-33ac447e668d", "248df4b7-aa70-47b8-a036-33ac447e668d")
     */
    public deleteSavedQueryDraft(
        harbor_id: string,
        saved_query_id: string,
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): core.HttpResponsePromise<Polytomic.DeletedHarborSavedQueryDraftEnvelope> {
        return core.HttpResponsePromise.fromPromise(
            this.__deleteSavedQueryDraft(harbor_id, saved_query_id, requestOptions),
        );
    }

    private async __deleteSavedQueryDraft(
        harbor_id: string,
        saved_query_id: string,
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.DeletedHarborSavedQueryDraftEnvelope>> {
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "Idempotency-Key": requestOptions?.idempotencyKey,
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/harbors/${core.url.encodePathParam(harbor_id)}/saved-queries/${core.url.encodePathParam(saved_query_id)}/draft`,
            ),
            method: "DELETE",
            headers: _headers,
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: _response.body as Polytomic.DeletedHarborSavedQueryDraftEnvelope,
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 409:
                    throw new Polytomic.ConflictError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 422:
                    throw new Polytomic.UnprocessableEntityError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "DELETE",
            "/api/harbors/{harbor_id}/saved-queries/{saved_query_id}/draft",
        );
    }

    /**
     * Validates and publishes the current draft as the next immutable saved-query version.
     *
     * Only organization administrators can publish. Publication executes and validates
     * the current draft with the Polytomic-managed Harbor's MotherDuck `read_scaling`
     * credential. It fails instead of using the Harbor writer credential when the
     * reader credential is missing or incomplete.
     *
     * After execution, publication verifies that the draft did not change. A
     * concurrent edit returns a conflict and remains a draft. Successful publication
     * creates the next immutable revision, records the observed ordered columns as its
     * output contract, removes validation inputs from the published definition, and
     * deletes the mutable draft. The response includes the immutable `revision_id`
     * that future executions use for exact provenance.
     *
     * @param {string} harbor_id - Unique identifier of the Harbor.
     * @param {string} saved_query_id - Unique stable identifier of the saved query.
     * @param {HarborsClient.IdempotentRequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.ConflictError}
     * @throws {@link Polytomic.UnprocessableEntityError}
     * @throws {@link Polytomic.InternalServerError}
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.publishSavedQueryDraft("248df4b7-aa70-47b8-a036-33ac447e668d", "248df4b7-aa70-47b8-a036-33ac447e668d")
     */
    public publishSavedQueryDraft(
        harbor_id: string,
        saved_query_id: string,
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): core.HttpResponsePromise<Polytomic.HarborSavedQueryEnvelope> {
        return core.HttpResponsePromise.fromPromise(
            this.__publishSavedQueryDraft(harbor_id, saved_query_id, requestOptions),
        );
    }

    private async __publishSavedQueryDraft(
        harbor_id: string,
        saved_query_id: string,
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.HarborSavedQueryEnvelope>> {
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "Idempotency-Key": requestOptions?.idempotencyKey,
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/harbors/${core.url.encodePathParam(harbor_id)}/saved-queries/${core.url.encodePathParam(saved_query_id)}/draft/publish`,
            ),
            method: "POST",
            headers: _headers,
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Polytomic.HarborSavedQueryEnvelope, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 409:
                    throw new Polytomic.ConflictError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 422:
                    throw new Polytomic.UnprocessableEntityError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "POST",
            "/api/harbors/{harbor_id}/saved-queries/{saved_query_id}/draft/publish",
        );
    }

    /**
     * Validates the exact current saved-query draft and returns a bounded ephemeral preview.
     *
     * Only organization administrators can validate drafts. Named template references
     * compile to DuckDB placeholders, and typed values are passed separately rather
     * than interpolated into SQL.
     *
     * The query runs with the Polytomic-managed Harbor's MotherDuck `read_scaling`
     * credential. Validation fails when that credential is missing or incomplete and
     * never falls back to the Harbor writer credential. Preview rows and serialized
     * response size are bounded. The preview and validation attempt are not persisted.
     *
     * @param {string} harbor_id - Unique identifier of the Harbor.
     * @param {string} saved_query_id - Unique stable identifier of the saved query.
     * @param {HarborsClient.IdempotentRequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.UnprocessableEntityError}
     * @throws {@link Polytomic.InternalServerError}
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.validateSavedQueryDraft("248df4b7-aa70-47b8-a036-33ac447e668d", "248df4b7-aa70-47b8-a036-33ac447e668d")
     */
    public validateSavedQueryDraft(
        harbor_id: string,
        saved_query_id: string,
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): core.HttpResponsePromise<Polytomic.HarborSavedQueryValidationEnvelope> {
        return core.HttpResponsePromise.fromPromise(
            this.__validateSavedQueryDraft(harbor_id, saved_query_id, requestOptions),
        );
    }

    private async __validateSavedQueryDraft(
        harbor_id: string,
        saved_query_id: string,
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.HarborSavedQueryValidationEnvelope>> {
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "Idempotency-Key": requestOptions?.idempotencyKey,
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/harbors/${core.url.encodePathParam(harbor_id)}/saved-queries/${core.url.encodePathParam(saved_query_id)}/draft/validate`,
            ),
            method: "POST",
            headers: _headers,
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: _response.body as Polytomic.HarborSavedQueryValidationEnvelope,
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 422:
                    throw new Polytomic.UnprocessableEntityError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "POST",
            "/api/harbors/{harbor_id}/saved-queries/{saved_query_id}/draft/validate",
        );
    }

    /**
     * Submits the current published Harbor saved query for asynchronous reader-only execution.
     *
     * Use a Harbor-bound scoped credential with query access to the Harbor's backing
     * Connection. Administrator credentials cannot submit executions here. Saved
     * queries require a Polytomic-managed Harbor.
     *
     * Polytomic selects the current published revision while processing your request.
     * The response identifies that exact immutable revision. Selection happens before
     * Polytomic accepts the execution. Drafts, unpublished queries, and archived
     * queries are unavailable for selection. You cannot select a historical revision
     * or supply SQL through this endpoint.
     *
     * > ⚠️ Concurrent publication changes
     * >
     * > Publishing, unpublishing, or archiving a saved query after selection does not
     * > change the selected revision. An in-flight request may still be accepted and
     * > execute that revision. Unpublishing or archiving does not cancel in-flight
     * > requests or queued executions. Query access is checked again before execution
     * > and result retrieval.
     *
     * Parameter values are bound separately from SQL. Omitted parameters use the
     * published `default_value`, never draft validation values. An omitted optional
     * parameter without a default binds SQL `NULL`. Required parameters allow omission
     * when a default exists, but reject explicit `null` even with a default. Missing
     * required values without defaults, explicit `null` for required parameters,
     * unknown parameter names, and invalid scalar types return
     * `422 Unprocessable Entity` without accepting an execution. Execution requires
     * reader credentials and never falls back to writer credentials.
     *
     * For report windows, define parameters with `required: true` and a
     * `default_value`, such as `7` for a `days` parameter. This allows omission while
     * preventing explicit `null` from turning a time filter into a SQL `NULL`
     * comparison.
     *
     * To archive a saved query through REST, use
     * [`DELETE /api/harbors/{harbor_id}/saved-queries/{saved_query_id}`](../../../../../../api-reference/harbors/delete-saved-query).
     * Unpublish is available only through the GraphQL `unpublishHarborSavedQuery`
     * mutation, not a REST endpoint.
     *
     * ## Results
     *
     * The response returns a task ID with status `created`, not result rows. Poll
     * [`GET /api/queries/{id}`](../../../../../../api-reference/query-runner/get-query) using a credential
     * from the same Harbor profile. Query access is checked again before execution and
     * result retrieval. Stop polling at `done`, `failed`, or `unknown`; `unknown` means
     * execution started but no durable terminal result is available.
     *
     * Results and detailed failure information expire after 24 hours. Use the
     * `expires` field and follow `links.next` to retrieve additional result pages.
     *
     * ## Harbor Activity
     *
     * Send a new nonzero UUID in `X-Polytomic-Activity-Request-ID`. Missing or invalid
     * IDs return `400 Bad Request`. Reusing a consumed ID returns `409 Conflict` and
     * does not create another execution.
     *
     * You may omit `X-Polytomic-Harbor-Session` for direct REST requests. If supplied,
     * the session must be active and bound to your credential and Harbor.
     *
     * Polytomic records `query.submitted` atomically with acceptance. The event
     * identifies the saved query, immutable revision, published version, and bounded
     * name snapshot. SQL, parameter values and defaults, column names, and result rows
     * remain outside Activity metadata. If Activity persistence is unavailable, the
     * request returns `503 Service Unavailable` without accepting an execution.
     *
     * `query.started` records provider invocation. `query.succeeded` means results are
     * available for authorized retrieval, not that you have consumed them.
     *
     * @param {string} harbor_id - Unique identifier of the Harbor.
     * @param {string} saved_query_id - Unique stable identifier of the saved query.
     * @param {Polytomic.ExecuteHarborSavedQueryRequest} request
     * @param {HarborsClient.IdempotentRequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.BadRequestError}
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.ConflictError}
     * @throws {@link Polytomic.UnprocessableEntityError}
     * @throws {@link Polytomic.InternalServerError}
     * @throws {@link Polytomic.ServiceUnavailableError}
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.executeSavedQuery("248df4b7-aa70-47b8-a036-33ac447e668d", "248df4b7-aa70-47b8-a036-33ac447e668d")
     */
    public executeSavedQuery(
        harbor_id: string,
        saved_query_id: string,
        request: Polytomic.ExecuteHarborSavedQueryRequest = {},
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): core.HttpResponsePromise<Polytomic.ExecuteHarborSavedQueryEnvelope> {
        return core.HttpResponsePromise.fromPromise(
            this.__executeSavedQuery(harbor_id, saved_query_id, request, requestOptions),
        );
    }

    private async __executeSavedQuery(
        harbor_id: string,
        saved_query_id: string,
        request: Polytomic.ExecuteHarborSavedQueryRequest = {},
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.ExecuteHarborSavedQueryEnvelope>> {
        const {
            "X-Polytomic-Harbor-Session": polytomicHarborSession,
            "X-Polytomic-Activity-Request-ID": polytomicActivityRequestId,
            ..._body
        } = request;
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "X-Polytomic-Harbor-Session": polytomicHarborSession,
                "X-Polytomic-Activity-Request-ID": polytomicActivityRequestId,
                "Idempotency-Key": requestOptions?.idempotencyKey,
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/harbors/${core.url.encodePathParam(harbor_id)}/saved-queries/${core.url.encodePathParam(saved_query_id)}/execute`,
            ),
            method: "POST",
            headers: _headers,
            contentType: "application/json",
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            requestType: "json",
            body: mergeAdditionalBodyParameters(_body, requestOptions?.additionalBodyParameters),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: _response.body as Polytomic.ExecuteHarborSavedQueryEnvelope,
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 400:
                    throw new Polytomic.BadRequestError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 409:
                    throw new Polytomic.ConflictError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 422:
                    throw new Polytomic.UnprocessableEntityError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 503:
                    throw new Polytomic.ServiceUnavailableError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "POST",
            "/api/harbors/{harbor_id}/saved-queries/{saved_query_id}/execute",
        );
    }

    /**
     * Resolves documented source table and field identities to the names a Harbor's backing Connection accepts.
     *
     * @param {string} harbor_id - Unique identifier of the Harbor.
     * @param {Polytomic.ResolveHarborSourceMappingsRequest} request
     * @param {HarborsClient.IdempotentRequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.UnprocessableEntityError}
     * @throws {@link Polytomic.InternalServerError}
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.resolveSourceMappings("248df4b7-aa70-47b8-a036-33ac447e668d")
     */
    public resolveSourceMappings(
        harbor_id: string,
        request: Polytomic.ResolveHarborSourceMappingsRequest = {},
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): core.HttpResponsePromise<Polytomic.ResolveHarborSourceMappingsEnvelope> {
        return core.HttpResponsePromise.fromPromise(this.__resolveSourceMappings(harbor_id, request, requestOptions));
    }

    private async __resolveSourceMappings(
        harbor_id: string,
        request: Polytomic.ResolveHarborSourceMappingsRequest = {},
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.ResolveHarborSourceMappingsEnvelope>> {
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "Idempotency-Key": requestOptions?.idempotencyKey,
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/harbors/${core.url.encodePathParam(harbor_id)}/source-mappings/resolve`,
            ),
            method: "POST",
            headers: _headers,
            contentType: "application/json",
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            requestType: "json",
            body: mergeAdditionalBodyParameters(request, requestOptions?.additionalBodyParameters),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: _response.body as Polytomic.ResolveHarborSourceMappingsEnvelope,
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 422:
                    throw new Polytomic.UnprocessableEntityError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "POST",
            "/api/harbors/{harbor_id}/source-mappings/resolve",
        );
    }

    /**
     * Returns raw Polytomic refresh evidence for datasets written to a Harbor.
     *
     * Each pipeline corresponds to a bulk sync or model sync that writes at least one
     * dataset to the Harbor's backing Connection. Tables populated outside Polytomic
     * are not included, even when they are queryable through a customer-managed
     * backing Connection.
     *
     * The response groups shared pipeline evidence so schedules and configuration are
     * not repeated for every dataset:
     *
     * - Each entry in `pipelines` identifies the producer through `type` and `id`.
     *   Separate pipelines targeting the same physical dataset remain separate
     *   entries.
     * - `datasets` is keyed by the effective destination dataset name.
     *   `last_success_at` is the start time of the most recent execution in which that
     *   dataset completed successfully, including a successful dataset within a bulk
     *   execution that completes with errors. The start time is a conservative upper
     *   bound because source reads and destination writes happen afterward.
     * - `latest_status` preserves the latest Polytomic execution status. Never-run
     *   datasets omit this field.
     * - Pipeline-level `schedules` preserves schedule parameters and selectors. A
     *   schedule can be manual, event-driven, advanced, selective, or limited to
     *   named source schemas, so callers should not reduce the list to one inferred
     *   cadence.
     * - Continuous schedules use the scheduler's persisted next firing. If scheduler
     *   state is unavailable, `next_run_at` is omitted rather than recalculated with
     *   new jitter.
     * - Paused pipelines remain present with `refresh_enabled` set to `false` and no
     *   `next_run_at`.
     *
     * Use absolute timestamps and the raw statuses to apply the maximum acceptable
     * staleness for your task. The endpoint does not classify datasets or the Harbor
     * as healthy, stale, or unhealthy.
     *
     * @param {string} harbor_id - Unique identifier of the Harbor.
     * @param {HarborsClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.InternalServerError}
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.getStatus("248df4b7-aa70-47b8-a036-33ac447e668d")
     */
    public getStatus(
        harbor_id: string,
        requestOptions?: HarborsClient.RequestOptions,
    ): core.HttpResponsePromise<Polytomic.HarborStatusEnvelope> {
        return core.HttpResponsePromise.fromPromise(this.__getStatus(harbor_id, requestOptions));
    }

    private async __getStatus(
        harbor_id: string,
        requestOptions?: HarborsClient.RequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.HarborStatusEnvelope>> {
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/harbors/${core.url.encodePathParam(harbor_id)}/status`,
            ),
            method: "GET",
            headers: _headers,
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Polytomic.HarborStatusEnvelope, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "GET",
            "/api/harbors/{harbor_id}/status",
        );
    }

    /**
     * Lists Harbor-only users assigned to a Harbor.
     *
     * The response contains only Harbor-only users currently assigned to this Harbor.
     *
     * @param {string} harbor_id - Unique identifier of the Harbor.
     * @param {Polytomic.HarborsListUsersRequest} request
     * @param {HarborsClient.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.UnprocessableEntityError}
     * @throws {@link Polytomic.InternalServerError}
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.listUsers("248df4b7-aa70-47b8-a036-33ac447e668d", {
     *         limit: 50,
     *         page_token: "page_token"
     *     })
     */
    public listUsers(
        harbor_id: string,
        request: Polytomic.HarborsListUsersRequest = {},
        requestOptions?: HarborsClient.RequestOptions,
    ): core.HttpResponsePromise<Polytomic.HarborUserListEnvelope> {
        return core.HttpResponsePromise.fromPromise(this.__listUsers(harbor_id, request, requestOptions));
    }

    private async __listUsers(
        harbor_id: string,
        request: Polytomic.HarborsListUsersRequest = {},
        requestOptions?: HarborsClient.RequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.HarborUserListEnvelope>> {
        const { limit, page_token: pageToken } = request;
        const _queryParams: Record<string, unknown> = {
            limit,
            page_token: pageToken,
        };
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/harbors/${core.url.encodePathParam(harbor_id)}/users`,
            ),
            method: "GET",
            headers: _headers,
            queryString: core.url
                .queryBuilder()
                .addMany(_queryParams)
                .mergeAdditional(requestOptions?.queryParams)
                .build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Polytomic.HarborUserListEnvelope, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 422:
                    throw new Polytomic.UnprocessableEntityError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "GET",
            "/api/harbors/{harbor_id}/users",
        );
    }

    /**
     * Invites and assigns a new Harbor-only user.
     *
     * The invited account is restricted to assigned Harbors and does not receive regular Polytomic application access.
     *
     * @param {string} harbor_id - Unique identifier of the Harbor.
     * @param {Polytomic.InviteHarborUserRequest} request
     * @param {HarborsClient.IdempotentRequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.ConflictError}
     * @throws {@link Polytomic.UnprocessableEntityError}
     * @throws {@link Polytomic.InternalServerError}
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.inviteUser("248df4b7-aa70-47b8-a036-33ac447e668d", {
     *         email: "analyst@example.com"
     *     })
     */
    public inviteUser(
        harbor_id: string,
        request: Polytomic.InviteHarborUserRequest,
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): core.HttpResponsePromise<Polytomic.HarborUserEnvelope> {
        return core.HttpResponsePromise.fromPromise(this.__inviteUser(harbor_id, request, requestOptions));
    }

    private async __inviteUser(
        harbor_id: string,
        request: Polytomic.InviteHarborUserRequest,
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.HarborUserEnvelope>> {
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "Idempotency-Key": requestOptions?.idempotencyKey,
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/harbors/${core.url.encodePathParam(harbor_id)}/users`,
            ),
            method: "POST",
            headers: _headers,
            contentType: "application/json",
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            requestType: "json",
            body: mergeAdditionalBodyParameters(request, requestOptions?.additionalBodyParameters),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Polytomic.HarborUserEnvelope, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 409:
                    throw new Polytomic.ConflictError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 422:
                    throw new Polytomic.UnprocessableEntityError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "POST",
            "/api/harbors/{harbor_id}/users",
        );
    }

    /**
     * Assigns an existing Harbor-only user to a Harbor.
     *
     * The assignment is idempotent. Regular Polytomic users cannot be assigned because they already have application access to Harbors.
     *
     * @param {string} harbor_id - Unique identifier of the Harbor.
     * @param {string} user_id - Unique identifier of the Harbor-only user.
     * @param {HarborsClient.IdempotentRequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.UnprocessableEntityError}
     * @throws {@link Polytomic.InternalServerError}
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.assignUser("248df4b7-aa70-47b8-a036-33ac447e668d", "248df4b7-aa70-47b8-a036-33ac447e668d")
     */
    public assignUser(
        harbor_id: string,
        user_id: string,
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): core.HttpResponsePromise<Polytomic.HarborUserEnvelope> {
        return core.HttpResponsePromise.fromPromise(this.__assignUser(harbor_id, user_id, requestOptions));
    }

    private async __assignUser(
        harbor_id: string,
        user_id: string,
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.HarborUserEnvelope>> {
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "Idempotency-Key": requestOptions?.idempotencyKey,
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/harbors/${core.url.encodePathParam(harbor_id)}/users/${core.url.encodePathParam(user_id)}`,
            ),
            method: "PUT",
            headers: _headers,
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Polytomic.HarborUserEnvelope, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 422:
                    throw new Polytomic.UnprocessableEntityError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "PUT",
            "/api/harbors/{harbor_id}/users/{user_id}",
        );
    }

    /**
     * Removes a Harbor assignment without deleting the user.
     *
     * This removes only the Harbor assignment. The organization user remains available and may retain assignments to other Harbors.
     *
     * @param {string} harbor_id - Unique identifier of the Harbor.
     * @param {string} user_id - Unique identifier of the Harbor-only user.
     * @param {HarborsClient.IdempotentRequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Polytomic.ForbiddenError}
     * @throws {@link Polytomic.NotFoundError}
     * @throws {@link Polytomic.UnprocessableEntityError}
     * @throws {@link Polytomic.InternalServerError}
     * @throws {@link errors.PolytomicError}
     * @throws {@link errors.PolytomicTimeoutError}
     *
     * @example
     *     await client.harbors.unassignUser("248df4b7-aa70-47b8-a036-33ac447e668d", "248df4b7-aa70-47b8-a036-33ac447e668d")
     */
    public unassignUser(
        harbor_id: string,
        user_id: string,
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): core.HttpResponsePromise<Polytomic.UnassignedHarborUserEnvelope> {
        return core.HttpResponsePromise.fromPromise(this.__unassignUser(harbor_id, user_id, requestOptions));
    }

    private async __unassignUser(
        harbor_id: string,
        user_id: string,
        requestOptions?: HarborsClient.IdempotentRequestOptions,
    ): Promise<core.WithRawResponse<Polytomic.UnassignedHarborUserEnvelope>> {
        const _authRequest: core.AuthRequest = await this._options.authProvider.getAuthRequest();
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            _authRequest.headers,
            this._options?.headers,
            mergeOnlyDefinedHeaders({
                "Idempotency-Key": requestOptions?.idempotencyKey,
                "X-Polytomic-Version": requestOptions?.version ?? this._options?.version ?? "2025-09-18",
            }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PolytomicEnvironment.Default,
                `api/harbors/${core.url.encodePathParam(harbor_id)}/users/${core.url.encodePathParam(user_id)}`,
            ),
            method: "DELETE",
            headers: _headers,
            queryString: core.url.queryBuilder().mergeAdditional(requestOptions?.queryParams).build(),
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return {
                data: _response.body as Polytomic.UnassignedHarborUserEnvelope,
                rawResponse: _response.rawResponse,
            };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 403:
                    throw new Polytomic.ForbiddenError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 404:
                    throw new Polytomic.NotFoundError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 422:
                    throw new Polytomic.UnprocessableEntityError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                case 500:
                    throw new Polytomic.InternalServerError(
                        _response.error.body as Polytomic.ApiError,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PolytomicError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        return handleNonStatusCodeError(
            _response.error,
            _response.rawResponse,
            "DELETE",
            "/api/harbors/{harbor_id}/users/{user_id}",
        );
    }
}
