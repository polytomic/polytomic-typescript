// This file was auto-generated by Fern from our API Definition.

import * as Polytomic from "../../src/api/index";
import { PolytomicClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("RecordViewLinksClient", () => {
    test("GetCapabilities (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new PolytomicClient({
            maxRetries: 0,
            token: "test",
            version: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = {
            data: {
                connection_id: "248df4b7-aa70-47b8-a036-33ac447e668d",
                default_field_policy: "default_field_policy",
                default_ttl_seconds: 1,
                lookup_key_field: "lookup_key_field",
                max_ttl_seconds: 1,
                reason: "reason",
                schema_id: "schema_id",
                supported: true,
                supports_live_refresh: true,
                supports_snapshot: true,
            },
        };

        server
            .mockEndpoint()
            .get("/api/record-view-links/capabilities")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.recordViewLinks.getCapabilities({
            connection_id: "248df4b7-aa70-47b8-a036-33ac447e668d",
            schema_id: "schema_id",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("GetCapabilities (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new PolytomicClient({
            maxRetries: 0,
            token: "test",
            version: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/api/record-view-links/capabilities")
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.recordViewLinks.getCapabilities({
                connection_id: "connection_id",
                schema_id: "schema_id",
            });
        }).rejects.toThrow(Polytomic.BadRequestError);
    });

    test("GetCapabilities (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new PolytomicClient({
            maxRetries: 0,
            token: "test",
            version: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/api/record-view-links/capabilities")
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.recordViewLinks.getCapabilities({
                connection_id: "connection_id",
                schema_id: "schema_id",
            });
        }).rejects.toThrow(Polytomic.ForbiddenError);
    });

    test("GetCapabilities (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new PolytomicClient({
            maxRetries: 0,
            token: "test",
            version: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/api/record-view-links/capabilities")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.recordViewLinks.getCapabilities({
                connection_id: "connection_id",
                schema_id: "schema_id",
            });
        }).rejects.toThrow(Polytomic.NotFoundError);
    });

    test("GetCapabilities (5)", async () => {
        const server = mockServerPool.createServer();
        const client = new PolytomicClient({
            maxRetries: 0,
            token: "test",
            version: "test",
            environment: server.baseUrl,
        });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/api/record-view-links/capabilities")
            .respondWith()
            .statusCode(500)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.recordViewLinks.getCapabilities({
                connection_id: "connection_id",
                schema_id: "schema_id",
            });
        }).rejects.toThrow(Polytomic.InternalServerError);
    });
});
